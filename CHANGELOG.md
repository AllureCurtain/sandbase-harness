# Changelog

## Unreleased

### Added

- Registers the documented `managed-agents environments` group. All six commands — `list`, `inspect`, `create`, `update`, `archive`, `worker-keys` — are implemented in `src/cli/runtime-management-commands.ts`, documented as covered in `docs/api-matrix.md:87`, and ticked off in `docs/spec/tasks.md:105`, but the module was imported by nothing, so every one of them answered `error: unknown command 'environments'`. This is the third instance of that pattern (#459/#463 worker, #460/#465 session) and the review that found it is the same one. The module also holds the `settings` group and **only the environments half is registered**, because driving the settings commands against the real API showed all three are broken — `settings get` throws reading a `model_provider` field the route does not return, `settings validate` throws iterating a `checks` array the route does not return, and `settings set-model` sends `PATCH /v1/x/settings`, a verb the route does not mount. Those are source changes rather than a registration, so they stay filed as **#466** and the `settings` command group deliberately remains unregistered here instead of being registered broken. Each environments command is driven against the **real** routes over a real HTTP listener and a real SQLite file, and every write is read back through a different handler than the one that wrote it. Two behaviours had to be measured rather than assumed. `archive` answers with the archived row itself, so `status` and `archived_at` are on the response the command already holds; and archiving is **terminal**: the row leaves the listing, a later single-resource read answers `404`, and a second archive is refused — shared `archiveResource` behaviour that also means a caller cannot use a repeat archive as a read-back, which is how the first draft of that test was wrong. `parseConfigJson` now names `--config-json` when the value is unparseable: it previously let the raw parser error escape, so `--config-json 'not json'` was reported as `Unexpected token 'o', "not json" is not valid JSON` with no indication of which flag was wrong, while the sibling shape check already named the option — the two refusals of one option now read alike. Docs updated in `docs/usage.md` (the CLI command list and a paragraph on `--hosting-type`, `--config-json`, partial updates, and the terminal archive).

- Registers the documented `managed-agents session` group. All five commands — `create`, `message`, `tail`, `inspect`, `logs` — are implemented in `src/cli/session-commands.ts`, documented as covered in `docs/api-matrix.md:84`, and ticked off in `docs/spec/tasks.md:102`, but the module was imported by nothing, so every one of them answered `error: unknown command 'session'`. Registering them was not sufficient, and the same review that made this the second instance of one defect is what turned it up: driving each command against the **real** routes over a real HTTP listener and a real SQLite file showed that `session create --agent <name>` is refused with `400 invalid_agent_ref`, because a session's `agent` field identifies an agent by **id** (`src/api/routes/sessions.ts:84`) and every published example passes `$AGENT_ID` or `agent_01J8XkN5uT3vHpLqRfWdY2`. The option's help text claimed "name or id", which was a claim the server refuses, so it now says id — and a name cannot simply be resolved, because migration M013 rebuilds `agents` without the `UNIQUE` that `name` originally carried, so a name does not identify one row. That refusal is pinned by a test rather than left implicit. `--no-stream` is also pinned by value, not by name: `sessionMessageCommand` branches on `opts.stream === false`, and that mapping cannot be read off `.opts()` before parsing, because commander applies the `--no-x` default during the parse — a pre-parse read omits `stream` entirely and makes a correct mapping look unverified. That was measured with a throwaway probe rather than assumed, and the probe was deleted. `session tail` is verified against the real SSE route with the log replayed from the start, and the test ends it by closing the connection because the command is documented as never exiting on its own; how that forced close surfaces (a transport `TypeError: terminated`) is deliberately **not** asserted, because a real client ends a tail with SIGINT and pinning the transport would pin the wrong thing. Docs updated in `docs/usage.md` (the CLI command list and a paragraph on `--agent`, `--no-stream`, and the output of each command). The same review found a second, separate defect worth its own Issue: the SDK drops the published error envelope's machine-readable `code` and `type` and keeps only `error.message` (`src/sdk/client.ts:327-332`), so a caller cannot branch on `invalid_agent_ref` without matching human text — asserted as absent in the new test so that fixing it fails the test rather than passing silently.

- Registers the documented `managed-agents worker poll` command. The self-hosted worker CLI is described in five places (`docs/deployment.md:234`, `docs/api.md`, `docs/api-matrix.md:63` and `:85`, and the Console's environment setup step) and was ticked off in `docs/spec/tasks.md:84`, but `workerPollCommand` was never registered in `src/cli/program.ts` — so the documented invocation answered `error: unknown command 'worker'`. Registering it was not sufficient: `POST /v1/x/worker/complete` requires `worker_id` (`src/api/routes/worker.ts:44`) and matches the row on it (`AND claimed_by = ?`), and the command never sent it, so the first claimed item would have been executed on the worker and then left `claimed`, with the completion answering `400` after the side effect had already happened — the worst of both, since the work was done and its result was lost. `workerId` was already computed and simply not threaded through, so the completion now carries the same identity the claim sent, and both the success and the `failed` branch report it. `--port` and `--interval-ms` are validated before the loop starts: the old `Math.max(250, Number(opts.intervalMs ?? 1000))` is `NaN` for an unparseable value and `setTimeout(fn, NaN)` fires immediately, so a typo became a busy loop against the server with no message. The module had **no test at all**, which is how it shipped dead and wrong at once; the new tests run the round trip against the **real** `workerRoutes` app over a real HTTP listener and the **real** `WorkQueue`, and read the queue row back from SQLite, because a fetch stub would agree with whatever the command happened to send. Docs updated in `docs/deployment.md` (an option table and the startup-refusal rule) and `docs/api.md` (`--once`, `--interval-ms`, `--worker-id`). The same review found that `src/cli/session-commands.ts`, `src/cli/runtime-management-commands.ts` and `src/cli/workspace-commands.ts` are imported by nothing either, so `managed-agents session ...`, `settings ...`, `environments ...` and `workspace ...` are documented in the same "CLI Coverage" table and equally unreachable; those are separate surfaces and are tracked separately rather than folded into this change.
- Honours the published `include_archived` parameter on the memory-store listing, the sibling of the vault listing fixed in the previous change. The published contract documents it for memory stores too (`管理智能体上下文/记忆存储.md:1206`: "默认排除已归档的存储；传递 `include_archived: true` 可将其包含在内"), with `?include_archived=true` as its worked example (`:1210`), and the parameter was read by nobody: the exclusion was hardcoded in the listing's `WHERE`, so the documented request was answered with a page that omitted exactly the rows the caller asked for. As with vaults the bug was one clause wide — `toMemoryStore` already labelled an archived store with `status: "archived"` and a non-null `archived_at`, so only the `WHERE` was missing. The reading is **not** a second copy: it moved to `src/api/routes/query-params.ts` and both collections call it, because a validation rule this specific kept in two places is two places for the accepted values to drift, and the second copy is the one that gets forgotten. The test compares the two collections' refusals to **each other** rather than each to a literal, which is what would notice such a drift. That the extraction changed no vault behavior is verified by the existing vault test file passing unchanged rather than by inspection. `include_archived=false` means the default; any other value — `1`, `yes`, an empty string — is a `400` naming the parameter and the accepted values and carrying no `data` field, so the refusal cannot be misread as an empty page; a repeated parameter with contradictory values is refused rather than resolved first-wins. Including an archived store in a listing is not an un-archive: `GET /v1/memory_stores/{store_id}` still answers `404` and archiving stays terminal. This listing still implements no pagination and still ignores the published `limit`/`page`, recorded in `contracts/anthropic-cma/memory-stores.md` as an open difference rather than folded in. Docs updated in `docs/api.md`, `contracts/anthropic-cma/memory-stores.md` and the Console API reference.
- Honours the published `include_archived` parameter on the vault listing. The published contract makes the archived half of a vault collection opt-in (`将工作委派给智能体/使用保管库进行身份验证.md:1119`: "默认排除已归档的记录（传递 `include_archived=true` 可将其包含在内）"), and the parameter was read by nobody: the exclusion was hardcoded in the listing's `WHERE` clause, so a caller who passed it received a page omitting exactly the rows they asked for and nothing in the response said the filter had been ignored — the failure mode `src/api/routes/query-params.ts` exists to name. The bug was one clause wide, because `toVault` already labelled an archived row with `status: "archived"` and a non-null `archived_at`; only the `WHERE` was missing. `include_archived=false` is accepted and means the default, so a caller-generated explicit `false` does not fail where `true` succeeds. Any other value — `1`, `yes`, an empty string — is a `400` naming the parameter rather than a silent fall-back to the default, which is the same rule the agent-update path already applies to a malformed concurrency precondition: a request that looks filtered must not be answered as though it were not. Sending the parameter twice with different values is refused too, and that half needed measuring rather than assuming — `c.req.query()` returns the **first** value for a repeated parameter, so a first-wins read of `?include_archived=true&include_archived=false` answers `200` and quietly picks one of the two values the caller sent; the check therefore reads `c.req.queries()`. Including an archived vault in a listing is not an un-archive: `GET /v1/vaults/{id}` still answers `404` for an archived vault and archiving stays terminal. Both the published `/v1/vaults` and the local `/v1/credential-vaults` prefix gain the parameter, because they are one router mounted twice. The listing still implements no pagination and still ignores the published `limit`/`page`, which is a separate gap recorded in `contracts/anthropic-cma/credentials.md` rather than folded into this change. Docs updated in `docs/api.md`, `contracts/anthropic-cma/credentials.md` and the Console API reference.
- Accepts the **published** name of the agent-update concurrency precondition, `version`, alongside the local `expected_version`. The published contract defines this field (`定义您的智能体/智能体设置.md:350`: "`version` 字段是可选的：提供它可实现乐观并发控制（不匹配时返回 409），省略它则无条件应用更新（最后写入者获胜）") and the published update example sends it (`:361`), so `version` was read by nobody and fell through to the unknown-field guard: the documented request was answered `400 {"error":{"type":"invalid_request_error","message":"Invalid agent update","details":[{"path":"version","message":"Unknown agent update field \"version\""}]}}`. That is why mounting the published verb was necessary but not sufficient — the example's body was refused field by field. Both spellings name one precondition and are read as one, so sending both with **different** values is refused rather than resolved by precedence: silently preferring one would apply an update the other value said not to, which is the lost write the field exists to prevent. Sending both with the same value is accepted, because there is then nothing to resolve. A present-but-malformed `version` is a `400` naming `version` rather than a silent downgrade to an unconditional update, which is the rule the local spelling already had and the published spelling must not be the hole in. A numeric string is accepted because the published example interpolates a shell variable. `version` stays a precondition and never becomes part of the stored definition, so a caller cannot force the stored version number through it. `expected_version` is not renamed or deprecated: it is the local spelling, the Console and SDK use it, and both spellings are read. Docs updated in `docs/api.md`, `contracts/anthropic-cma/agents.md` and the Console API reference.
- Mounts `POST /v1/agents/{id}`, the **published** agent-update verb, on the same handler as `PUT`. Both published update examples send a body with `curl -d` and no `-X` (`定义您的智能体/智能体设置.md:354` and `:465`), which curl issues as `POST`, while the same file spells `-X POST` out explicitly for archive (`:576`) — so the omission is the verb, not an abbreviation — and no `PUT` or `PATCH` spelling appears anywhere in the published documentation set. This runtime mounted only `PUT`, so a caller who followed the contract built a request the documentation said would work and got a `404 {"error":{"type":"not_found","message":"No route matches this request"}}`. This contract already treats the verb as part of an endpoint's identity (`contracts/anthropic-cma/routes.md:24`: "A documented endpoint is one verb on one path"), and its second rule names this failure as the worse one — a route the documentation promises but the runtime does not mount is discovered from a 404 after the request was built. Both verbs take one handler reference rather than a second copy, because they are one operation. A contract sentence claiming "`PUT` and `PATCH` share one partial-update implementation" was wrong in the same place and is corrected here rather than made true by adding a `PATCH` route: no `PATCH` is mounted and none is published. `docs/api.md`, `contracts/anthropic-cma/routes.md`, `contracts/anthropic-cma/agents.md` and the Console API reference all gain the verb. Registering it does **not** make the published example work end to end on its own — the example also sends the concurrency field as `version`, which this runtime refuses as an unknown field in favour of `expected_version` (`智能体设置.md:350`), and that is a separate change on the request body.
- Publishes `deployment.paused` and `deployment.unpaused` to webhook subscribers. Pausing a deployment was durable and silent: the route set `status` and `paused_reason` and returned, so a subscriber that asked to be told when a deployment stops producing runs had to poll — the thing webhooks exist to remove. The published event table lists both names (`将工作委派给智能体/订阅Webhook.md:60-61`). Each carries `data: {type: 'deployment', id}`, which is a **reference rather than the object**: the published contract says a body carries the event's `type` and `id` and the receiver fetches current state itself (`:9`), which is also what keeps a retry from carrying a snapshot the resource has moved past. **Nothing is published when the call changes nothing.** The pause route is idempotent on purpose, so a repeat pause is the ordinary retry path rather than an edge case, and the published table states the same rule for the nearest comparable event — re-archiving an archived environment emits nothing (`:79`) — so reusing it is not inventing a second rule. A subscriber that cannot be reached does not fail the call either: the state change is committed first and the failed attempt is recorded as a delivery row for the retry sweep, because a receiver that is down turning a completed control-plane change into an error is how an operator loses a pause they did perform. Events had one source before this. Session events reach webhooks through a broadcast listener the runtime installs, which fires and forgets because it is on the hot path of every session event; operations events are not session events and have no such channel, so the route that causes one publishes it through one shared helper (`src/api/routes/operation-events.ts`) that **awaits** the attempt — these are rare calls, and waiting means the delivery row exists before the caller is told the change succeeded. Both sources end in the same dispatcher, so matching, signing, retries, and delivery rows behave identically, and the signing-key derivation now has one definition instead of two copies that had to agree. The published description of `deployment.paused` also covers an **automatic** pause after a non-recoverable trigger failure, and that cause still does not exist — it needs the failure taxonomy of the run path that `M042`'s own comment records as arriving with it — so the publishing path was built ahead of it deliberately, and the automatic pause must raise its event through this same path rather than beside it. The rest of the published event table remains unproduced (`agent.*`, `environment.*`, `vault.*`, `vault_credential.*`, the other `deployment.*` names, and `deployment_run.*`); a subscription listing those names is accepted and silent, which `operations.md` §4 records, because refusing an unknown name would break a subscription created from the published list. The delivery envelope itself is also unchanged, so a receiver written for the published envelope still cannot read a local delivery — the new events carry the `data` shape that envelope should already have, which makes the envelope change a purely envelopular one later.

- Serves deployment runs at the top level, where the published contract puts them. `GET /v1/deployment_runs?deployment_id=...&has_error=true` and `GET /v1/deployment_runs/{deployment_run_id}` now answer in the published object shape, with `deployment_id`, `trigger_context`, `session_id`, `error`, `agent`, and `created_at`. Both calls were previously **404**: the rows have been written by the scheduler since it was built, but they were reachable only nested under one deployment, in a shape that renamed five fields and carried `error` as a bare string. The item route is not a convenience — a `deployment_run` event carries a run id in `data.id` (`高级编排/定时部署.md:484`), so a runtime that can publish that id but not resolve it hands the caller an identifier that goes nowhere. This is a **read view**: `schedule_id` → `deployment_id` and `started_at` → `created_at` happen on the way out only, and no write path or stored column changes. `agent` is joined from the session the run created, so it is the agent that ran **and the version it ran as** — the deployment itself does not carry that version. Three parts are deliberately partial and recorded as such rather than filled in: `error.type` is a single local `deployment_run_failed` because session creation throws free-text messages and classifying them would mean matching on strings, so the published **shape** is honoured and the published **vocabulary** is not invented; `trigger_context.scheduled_at` is omitted because the runtime records when a run started, not when its trigger was due, and reporting the former as the latter would answer a schedule question with an execution fact; and a run that failed before a session existed reports `agent.version: null` with the deployment's current `agent_id`, because no version was recorded to report. `has_error` with any other value is refused by name rather than ignored, so a filtered question is never answered with an unfiltered list. The collection is also served under `/v1/x/deployment_runs` with the older envelope, and the nested route keeps its original field names for existing consumers — with a test pinning the two projections together so they cannot drift.

- Lets a scheduled deployment be paused and resumed over HTTP, and makes a paused deployment runnable by hand. `POST /v1/deployments/{id}/pause` stops the schedule from producing timed runs and records `paused_reason: {"type": "manual"}`; `POST /v1/deployments/{id}/unpause` clears it and resumes from the next scheduled instant. Both are published actions (`高级编排/定时部署.md:494`, `:539`), reachable at the local `/v1/scheduled-deployments*` spelling too, because the deployment router is one factory mounted twice. Sessions already running keep running, and `run-due` skips a paused deployment — that half already worked, since `runDueScheduledDeployments` selects `status = 'active'`. **The part that did not work was the manual run, and it was a live contract violation rather than a missing feature**: `POST /{id}/run` refused any status but `active`, and the only other reachable status is `paused` — the one the contract says must still run, "暂停期间仍允许通过 `run` 端点进行手动运行" (`定时部署.md:490`). Pause therefore meant "cannot be run at all", leaving an operator no way to drain a paused deployment before fixing it. The guard is removed rather than narrowed, because it was unreachable for every other value: the query already excludes archived rows and `normalizeScheduleStatus` maps everything that is not `paused` to `active`, so an `=== 'archived'` check would be dead code asserting nothing. Unpausing deliberately does **not** catch up missed triggers — the contract says so, and it is why a stored `next_run_at` that has already passed is recomputed forward while one still in the future is left alone; without that rule, a deployment paused over a weekend would fire every missed run the moment it resumed. `paused_reason` is a new column (`M042`), derived from `status` on the create and update routes as well as on the pause route, so a deployment cannot read as paused with no reason simply because it was paused through a different path, and a row paused before the migration upgrades with `null` rather than a back-filled `manual` the runtime never observed. The contract's other reason kind — an automatic pause after a non-recoverable trigger failure — is **not** implemented and is recorded as a gap, so `paused_reason` currently only ever holds `manual`.

- Serves scheduled deployments at the published `/v1/deployments*` paths as well as this runtime's own `/v1/scheduled-deployments*`. The published contract addresses this resource as a *deployment* at `/v1/deployments`, so a client generated from that documentation called `POST /v1/deployments` and received a 404 for a resource this runtime fully implements — and the body was not the documented error shape, so an SDK's error decoder fails while parsing and hands the caller an untyped transport error instead of a `NotFoundError` they can branch on. Both prefixes are now served by **one** router mounted twice, so the two cannot diverge route by route and a route added later is reachable at both with no second edit; the local spelling is untouched rather than deprecated, redirected, or removed, and a deployment created through one spelling is the same deployment read through the other. This one needed more than a remount. Vaults could be aliased in place because their router served exactly one resource family; `operations.ts` serves four (`/webhooks`, `/scheduled-deployments`, `/outcomes`, `/sessions/:id/outcomes`) from one router mounted at `/v1` and mirrored at `/v1/x`, so making its paths relative and remounting it would have put `/webhooks` and `/outcomes` under `/v1/deployments/` too. The deployment routes were therefore **extracted** into `src/api/routes/deployments.ts` with paths relative to their mount, and that module is imported and mounted twice from inside `operationsRoutes` — which is also what preserves the existing `/v1/x/scheduled-deployments` mirror and gives `/v1/x/deployments` the same legacy collection envelope as its twin, instead of the canonical one. The published verb for an update is `POST /v1/deployments/{id}` while this runtime uses `PUT`; the verb is deliberately **not** aliased, because changing what a verb does is a different change from adding a path, so that one call still has no route and `operations.md` §4 records it. `tests/unit/deployment-path-parity.test.ts` asserts the two prefixes expose the same route set in both directions, that every one of these routes is still registered by the module that owns them, and that no other family came along; `tests/integration/deployment-path-aliases.test.ts` asserts the shared-resource behaviour, the unchanged refusals, and both collection envelopes over HTTP.

- Serves credential vaults at the published `/v1/vaults*` paths as well as this runtime's own `/v1/credential-vaults*`. The published contract addresses vaults at `/v1/vaults`, so a client generated from that documentation called `POST /v1/vaults` and received a 404 for a resource this runtime fully implements — a failure that is worse than it looks, because the body was not the documented error shape and an SDK's error decoder fails while parsing it, handing the caller an untyped transport error instead of a `NotFoundError` they can branch on. Both prefixes are now served by **one** router mounted twice: the vault router declares its paths relative to its mount (`src/api/routes/credential-vaults.ts`) and `src/api/routes/resources.ts` mounts that same factory at both prefixes. That matters beyond brevity — a curated per-route alias list is exactly the kind of thing that drifts, leaving a route that answers for one caller and 404s for another on the same resource, so the alias is a mount and a vault route added later is reachable at both with no second edit. It also means the local spelling is untouched rather than deprecated, redirected, or removed: the Console, the TypeScript SDK and existing stored references all keep working, and a vault created through one spelling is the same vault read through the other. The published prefix is a CMA resource path like any other, so both spellings require the same `managed-agents` beta and the published one does not inherit the memory-store exception. The local management extensions (`rotate`, `mark-used`, both `audit` routes) are reachable at the published prefix too, because the alias is a mount rather than a curated list. `tests/unit/vault-path-parity.test.ts` asserts the two prefixes expose the same route set in both directions, with anchors so it cannot pass by finding nothing, and `tests/integration/vault-path-aliases.test.ts` asserts the shared-resource behaviour over HTTP.

- Lets an operator bound how long a session waits for an answer, so an unattended deployment can end a session nobody is coming back to answer instead of retaining its workspace forever. A session parked on a blocking call does not run again until the caller answers, so nothing inside the turn machinery can notice that the answer is never coming; previously the only way out was an operator stopping the session by hand. `loop_engine.options.requires_action_timeout_seconds` is now that bound, and a session parked longer than it is ended: a `session.error` carrying `requires_action_timeout` with `retry_status: "not_retryable"` is appended and the session reaches `timed_out`, published as `session.status_terminated`. That terminal status already existed, already meant exactly this, and was already the target of the Pi child's own timeout code — it simply was not reachable from `requires_action`, which is the one state that waits on a caller, so the transition table gained that one entry. **The default is unchanged and stays that way on purpose**: the published contract says a parked session "会话会无限期等待响应" — waits indefinitely (`权限策略.md:668`) — so the key is optional with no default, an absent key keeps the published behaviour exactly, and the bound is recorded in `sessions.md` §4 as a local extension rather than presented as published. Three rules come with it. It is measured from the event that parked the session, not from when a sweep noticed: a bound derived from the observer's clock would restart on every tick and reset across a restart, so the longest waits — the ones an operator most wants bounded — would be the ones that never expired. It never fires on a session at its spending ceiling, because a ceiling here refuses the next work-starting event **without changing the session's status**, so such a session is still `requires_action` and still parked, and its answer is a settlement event the budget deliberately still accepts — ending it would destroy work the runtime had explicitly promised to keep accepting. And it does not answer the parked calls: the session stops waiting, and nothing enters the append-only log that the caller did not send. The check reads `SessionManager.isBudgetExhausted` and the parked set from `parkedCalls`, the same definitions the budget refusal and `stop_reason.event_ids` use, so the bound cannot disagree with either about what the session was waiting on or whether it was out of budget. It is offered on both loop engines, since both can park, and it is not reachable from an agent definition, so no agent can widen or remove its own bound. An unrecognized value is refused by name at that path instead of being coerced; a bound that was saved but never activated has no effect.

- Accepts `user.steer` as its own session event, so a caller can steer a turn that is already running instead of waiting for it or starting another one. A message is queued on the session's serialized execution chain and starts a turn, so routing a steer through it would apply the instruction only after the turn it was meant to influence had ended — the runtime would then have reported a delivery that changed nothing. The event is therefore never turned into a `user.message`: it carries `input_id`, `text`, and an optional `expected_turn_id`, and it is written to the live engine session's own input channel, never to the execution chain, so it can carry text to a running turn and nothing else — it cannot start work, expose a tool, or change the tool policy the child was launched with. The answer carries a receipt the caller can act on: `delivered`, `duplicate` (the same `input_id` with the same text, which the engine is not told twice), `conflict` (the same `input_id` with different text, refused rather than merged into the earlier steer), `rejected` (no live session was accepting steering, so resending is safe), or `outcome_unknown` (the write was not acknowledged and must never be replayed, because the child may already have acted on it). `accepted` is `false` for `rejected` and `conflict` only, since reporting an unacknowledged write as a failure would invite exactly the replay the contract forbids. Admission is fail-closed rather than queued: at most one steer is in flight per turn, a steer arriving with no turn in flight or for a child that is gone is refused rather than buffered for a later turn, an `input_id` already spent on other text is refused, and `expected_turn_id` must name the active turn when it is present. Steer admission closes before the turn's `turn_complete` is published and receipts already accepted settle before it, so a turn completion cannot overtake a steer it still owes an answer to. The receipt is persisted in the event's `metadata` (`input_id`, `steer_state`, and `turn_id` / `detail` when present) so a client reading the log back learns what the engine actually did, the SDK exposes the same call as `sessions.steer(id, { inputId, text, expectedTurnId? })`, and a malformed steer is refused with `400 invalid_request` naming the field rather than recorded.

- Lets an operator preauthorize a gated Pi tool decision once. A gated native tool call had exactly one way to be answered — a person's `user.tool_confirmation` — so an unattended session stopped dead at the first `always_ask` call. `loop_engine.options.approval_mode` now selects the second way: `interactive` (the default, and the resolution for an omitted key) keeps waiting for a person exactly as before, while `preauthorized_once` lets the runtime answer the call itself under the platform-owned rule that mode installs. The decision travels the same one-shot path a person's does — the conditional consume on the durable `pi_tool_interactions` row is still the only thing that authorizes execution — and it is recorded with `decision_source: "platform"` and published as `requires_confirmation: false` with `confirmation_source: "platform"`, so no client can read an automatic decision as a click by a person and the session never reports `requires_action` for it. Nothing about the mode is standing or widening: the next gated call is decided again, the mode is read per gate so turning it off is honored by the following decision while a decision already recorded keeps its source, the compiled `--tools`/`--exclude-tools` plan and the gated name list are untouched, a call the rule does not name keeps waiting for a person rather than being denied unattended, and every path that cannot consume a trustworthy decision still denies. The setting is validated like the other runtime settings, so an unrecognized mode name is refused at `loop_engine.options.approval_mode` instead of being coerced to the default, and the Console loop-engine form offers it as a select. No agent definition can reach the key.

- Stops a declared outcome at the session's spending ceiling. The ceiling was enforced only where events are admitted, and an outcome's revisions are not events: `needs_revision` appended its feedback and re-entered the executor inside the declaration that had already been admitted, so a session could spend past its `max_list_cost` for as many iterations as it declared. The loop now reads the session's spend before it spends anything — the grading pass included — and closes the outcome with `result: "budget_reached"` on its terminal `span.outcome_evaluation_end`, the same name admission uses for the refusal, with no `session.error`. The published consequence the budget contract already relied on, "the next model request does not start", now covers the work one admitted event can keep starting rather than only the next event. Either way the session settles as it did before: a further work-starting event is still refused with `budget_reached` and settlement events are still accepted.

- Drives a declared outcome with its own revision loop instead of grading it once. `needs_revision` reported that work remained, but nothing acted on it: the verdict was published and the outcome stopped there, so a deliverable that nearly met its rubric ended exactly like one that met it. A revision verdict now appends the grader's explanation to the session log as a real `user.message` and runs another turn inside the same outcome, so the revision is visible in the event listing and the next turn reads its instruction from the log rather than from memory. The loop is bounded by the declared `max_iterations`, whose last allowed evaluation reports `max_iterations_reached` instead of asking for a revision that cannot run and still leaves the agent one final turn to settle its answer; it ends at the first `satisfied` or `failed`, and every iteration is observable as one revision message plus one `span.outcome_evaluation_start` / `_ongoing` / `_end` triple. An interrupt closes the outcome with a further end event carrying `result: "interrupted"` and an empty `outcome_evaluation_start_id` — the close is not tied to one evaluation — and records no `session.error`, and an interrupt that lands during a turn stops the loop before that turn is graded. A runtime that composes no grader now refuses `user.define_outcome` at admission with `outcome_grader_unavailable` (400) on both ingress paths instead of accepting an outcome it can never evaluate. Two rules are local and named in the contract: grading stays model-backed, and a revision turn that stops for a tool confirmation ends the outcome as `interrupted`, because the loop cannot drive another turn while the session is waiting on a human. The capability matrix moves `outcome-grading` from `partial` to `supported`.

- Grades a session's declared outcome and publishes the evaluation on the session's event log. `user.define_outcome` was durable and unread: it was admitted, defaulted and projected back onto the event listing, but the projection never put it into the agent's context, so the turn it queued ran with no criteria and nothing ever measured the result against the rubric — `max_iterations` and the two iteration numbers in `src/core/outcomes/contract.ts` had no reader at all. The event now projects into the turn as the outcome description plus the rubric, and once that turn completes the runtime appends `span.outcome_evaluation_start`, `span.outcome_evaluation_ongoing` and `span.outcome_evaluation_end` to the same log, the last carrying `result` (`satisfied | needs_revision | failed`), an explanation, and the id of the start event it closes. The grader runs through the model provider in its own context window over what the agent produced — its messages, tool calls and their results — and deliberately not over the system prompt, the lifecycle events, the outcome instruction, or an earlier verdict, because an evaluation anchored to the runtime's own scaffolding is not measuring the deliverable. The rubric comes from the declaration: inline text directly, a `{type: "file"}` rubric read from the upload the Files API stored, and a rubric file that cannot be read refuses the evaluation with `outcome_rubric_file_not_found` rather than grading against nothing. A grader that cannot run — no provider configured — closes the end event as `failed` and surfaces `session.error` with `outcome_evaluator_unavailable` and `retry_status: not_retryable`, so an ungraded outcome is never indistinguishable from a failed one; the end event is appended on every path so a client waiting on it cannot hang. The revision loop is not part of this change — `needs_revision` does not yet start another turn and `max_iterations` bounds nothing — and the capability matrix records the area as `partial` rather than claiming a delivered loop.

- Runs a session with part of its agent configuration replaced, without versioning the agent. `POST /v1/sessions` now accepts the third published form of `agent` — `{"type": "agent_with_overrides", "id", "version"?, ...}` — and resolves the overridable fields (`model`, `system`, `tools`, `mcp_servers`, `skills`) per field: omitted inherits the referenced version, `null` (or `[]` for a list) clears it for this session, and a value replaces it wholesale. Overrides never merge, so a `tools` override has to list every tool. The resolved configuration is what the session freezes as its own snapshot — the same `sessions.agent_definition` column a version pin already used — so the loop reads the overridden model and tool set, and capability admission judges that definition rather than the base agent, which closes the gap where an override could pass a gate on the agent and then execute something the gate never saw. The agent row, its version list, and its read-back are untouched. A refused override is a code-carrying 400 raised before the session row exists, so nothing is created: `agent_model_required` for `model: null`, `agent_tools_cleared_with_skills` for clearing `tools` while skills remain, `agent_mcp_server_not_found` for a definition that binds an `mcp_toolset` to an undeclared server, `invalid_agent_override_field` and `invalid_agent_overrides` for a malformed or unknown field, and the reference refusals `invalid_agent_ref` / `agent_required` now separate "malformed" from "missing" where both used to answer "agent field is required". Two rules are recorded as local rather than presented as published: an `mcp_toolset` cross-check runs on the resolved definition, so a `tools` override that introduces an undeclared binding is refused instead of persisting a toolset that silently does nothing, and `model.effort` is refused because no local provider executes effort and a session snapshot is projected without the field. `POST /v1/runs` still accepts only the pinning forms and refuses the override form by name rather than accepting it and ignoring it.

- Accepts the canonical `user.define_outcome` event. `contracts/anthropic-cma/sessions.md` §2 already stated that creation admits it alongside `user.message` and that its payload is defaulted, but the whitelist held `user.message` alone, so the contract described an event the runtime refused with `invalid_initial_event_type`. The event now validates and normalizes on both ingress paths — `initial_events` on creation and `POST /v1/sessions/{id}/events` — requiring a `description` and a `rubric` (inline `text` or a `file` reference), defaulting `max_iterations` to 3 and rejecting anything outside 1..20 rather than clamping it. The admitted payload has to survive the log, which has no per-type payload column, so it rides in `metadata` and `toApiEvent` lifts `description` / `rubric` / `max_iterations` back to top-level fields — without that the outcome would be written and then unreadable after a restart. A malformed live payload answers `400` with `invalid_define_outcome` and is not stored. Two document corrections came with it: §2 named the wrong code for a type outside the whitelist (the runtime answers `invalid_initial_event_type`), and `docs/api.md` / `docs/api-matrix.md` described `initial_events` as `user.message`-only.

- Serves the canonical cursor envelope from the session listing, which completes the conversion: every canonical `/v1` collection now answers with `{data, prev_page, next_page}`. `/v1/sessions` was the last one, and it could not take the null-cursor step because it is windowed — its `page` parameter was a plain number, so a caller had no way to carry the ordering and filter they had asked for, and the contract's claim that a cursor carries `{order, filter, page}` described a cursor that did not exist. `page` is now that cursor: it encodes `created_at DESC` and the normalized `agent_id` / `status` the query filtered on, `prev_page` and `next_page` are issued around the current page, and a value that is not one of this collection's cursors — including the page numbers callers used before, and a cursor replayed under a different filter — is refused with `400` rather than read as "page one". The SDK's `sessions.list` takes a cursor and the Console fetches sessions through `getCursorPage`, so neither still advertises `has_more`. The contract lost its last §4 row about `/v1` collections on the local envelope, gained a correction (it listed `include_archived` among the bound filters, a parameter this listing does not have), and the work-item listing remains the only recorded extension shape.

- Serves the canonical cursor envelope from the SDK-typed collections, and updates the SDK and Console types that described the old wire. `agents` (with its versions), `api-keys`, `environments` (with its worker keys) and `files` returned their whole result set through `pageOf`, so the canonical `/v1` prefix answered with the extension envelope while `docs/api-matrix.md` and the pagination contract described cursors. All six call sites now go through `cursorPageOf` with `{data, prev_page: null, next_page: null}`. The declared types moved with them, because a type that still names `has_more` describes a response the runtime no longer sends: the SDK's `agents.list` / `agents.versions` / `apiKeys.list` / `environments.list` / `environments.workerKeys` / `files.list` return `{data, prev_page, next_page}`, the Console gained `CursorPage<T>` and a `getCursorPage` helper and uses it for every collection the contract has converted, and the SDK's unit-test mocks answer the canonical shape. `sessions` is now the only canonical collection still on the local envelope — its window is a 1-based page number, so converting it means issuing a real cursor — and the work-item listing is recorded as a windowed extension shape that carries a `counts` object, which neither envelope describes. `pagination.md` §2, §4 and §6 and the capability matrix carry the resulting lists.

- Serves the canonical cursor envelope from the two windowed collections that could not take the null-cursor step. `/v1/skills` returned the local `has_more` / `first_id` / `last_id` fields **and** a `next_page` cursor in one body — the failure mode `contracts/anthropic-cma/pagination.md` §5 names, where a client reading `next_page` and a client reading `has_more` paginate by different rules — and the credential audit listings truncated by `limit` with nothing to continue from, so `has_more: false` described a page whose rows had been cut. Both now carry a real cursor: an offset bound to the filter that produced it (the `source` for skills, the vault/credential scope for the audit trail), so a caller can walk forward, walk back through `prev_page`, and is refused with a `400` when the cursor is malformed or belongs to another query. The audit trail needed a store change to be honest: `listCredentialAuditEvents` accepts an `offset` (its ordering already ends in `rowid DESC`, so a page boundary is stable) and the routes read one row beyond `limit` to decide whether a next page exists. `pagination.md` §2 lists the converted collections, §4's two temporary difference rows are gone, and the still-local row names the five SDK-typed collections.

- Serves the canonical cursor envelope from the resource collections that return their whole result. `contracts/anthropic-cma/pagination.md` §2 maps the two envelopes to `/v1` and `/v1/x`, and the Vault, credential, memory-store and memory listings were built with `pageOf`, so they answered with the extension shape on the canonical prefix. All four now go through `cursorPageOf` with `{data, prev_page: null, next_page: null}`, which the contract documents as the honest page for a complete set: a synthetic cursor would let a caller follow it into an empty page. Two neighbouring cases were deliberately left alone and are now recorded as differences rather than left implicit: the credential audit listings truncate by `limit` with no continuation, so neither envelope describes them truthfully yet, and `/v1/skills` returns the local fields **and** a `next_page` cursor in the same body — the exact "client reads one field while the server paginates by another" failure the contract names — which needs a followable cursor rather than the null-cursor step these four took. The collections the SDK types (`agents`, `api-keys`, `environments`, `files`, `sessions`) are still on the local shape and are named in the contract's difference table.

- Serves the canonical cursor envelope from the operations collections under `/v1`. `contracts/anthropic-cma/pagination.md` §2 and the capability matrix both state that canonical `/v1` collections return `{data, prev_page, next_page}` while `/v1/x` keeps the local `{data, has_more, first_id, last_id}`, and `collectionPager` existed as "the single place the surface chooses its envelope" with no caller at all. Every operations collection was built with `pageOf`, so `/v1/webhooks`, `/v1/scheduled-deployments`, `/v1/outcomes`, their nested `deliveries` / `runs` and the session outcome listing answered with the local shape under both prefixes. `operationsRoutes` now takes a `pageShape` and the server passes `canonical` for `/v1` and `legacy` for the `/v1/x` mirror, so the two prefixes differ by mount rather than by endpoint and no handler can emit both spellings at once; an action that answers `202` with a collection keeps its status. The resource collections (`agents`, `skills`, `api-keys`, `environments`, `files`, `credential-vaults`, `memory_stores`, `sessions`) still return the local envelope on both prefixes, which the contract now records as a named difference, because converting one changes a shape the SDK types and the Console read.

- Reconnects a session's MCP transports when the Vault credential they authenticate with is rotated. `contracts/anthropic-cma/credentials.md` §2 promised that "when the rotated Vault is referenced by active Sessions, the runtime asks each Session's MCP manager to close and reconnect its configured transports", and a previous replay had to drop the assertion because no such path existed: the rotate route replaced the ciphertext, appended its audit event and returned, while a server process that had already started kept the environment or the header it was connected with — and, because the redactor resolves the credential per call, the stale value was reported to the model verbatim once the old secret was no longer the one the session held. `McpManager.refreshAllCredentials` now closes every client and reconnects from the declared configs so the resolver is consulted again, `ToolResolver` refreshes the session's cached tool map, the session manager asks every non-terminal session whose `vault_ids` include the rotated vault, and the rotate route calls it after the rotation is committed. Tool wrappers the strategy already holds stay valid because they delegate through the manager's live tool map, so nothing has to rebuild the tool set. A reconnect failure is reported rather than rolled back, which is the rule the contract states and `GET /v1/x/mcp/status` is what shows a degraded server.

- Attaches a credential keyed by `mcp_server_url` to the MCP server it names. `contracts/anthropic-cma/credentials.md` §2 describes the keying and its canonicalizing comparison as an authentication path, `mcpServerUrlMatches` implemented the rule, and nothing applied it when a connection was made: `McpManager` built an `SSEClientTransport` with no headers at all, while the injection boundary put every `bearer_token` row in the vault into `request_headers` for whichever caller asked — so a token minted for one endpoint was available to every other one, and none of them received it. The boundary now skips a URL-keyed credential unless the caller declares that server, skipped rather than denied because such a credential is inapplicable to this call rather than refused for it, and the check sits above decryption so its secret is not even unwrapped. `ToolResolver` passes the declared URL as the target host and as the server key, and the manager presents the resolved headers on both legs of an SSE connection — the initial request through `eventSourceInit` and every message POST through `requestInit` — with the resolver's record emptied once the connection is established. Two shapes are covered rather than one: a `static_bearer` and the stored `access_token` of an `mcp_oauth` credential, which this runtime does not refresh but does hold. Because neither canonical MCP type records an `injection_location`, an empty stored token list on a keyed credential is read as "not specified" and presented as a header, while a list that was given is respected, including one that enables the body only.

- Resolves a session's Vault credentials in the runtime that serves sessions. `contracts/anthropic-cma/credentials.md` §2 states that `DefaultSessionExecutor` resolves the session's vaults once per turn, and `tests/integration/credential-execution.test.ts` proves the executor does exactly that, but the production assembly never supplied the resolver: `resolveCredentialInjections` is optional on the executor, `createRuntimeSessionServices` passed no such option, and `git grep -n 'resolveSessionCredentialInjections' -- src` matched only the credential boundary itself and the handoff-bundle report. In a CLI-started runtime the field was therefore `undefined`, so no vault environment reached a sandbox command or an MCP server and nothing a tool returned was scrubbed. `src/index.ts` now builds the resolver from that same boundary — with the workspace data directory and no target host, because a shell command names none — and the session services pass it to the executor. A host that assembles those services without a credential store keeps the previous behaviour of running sessions that hold no vault.

- Passes a session's Vault environment to the stdio MCP servers it declares. `contracts/anthropic-cma/credentials.md` cited `tests/integration/mcp.test.ts` for "a real stdio MCP server receives the session's Vault environment credential", and `credentials.md` §3 listed that path as aligned, but `McpManager` had no way to be handed a credential: it started every server with the `env` the agent definition declared and nothing else, so a toolset whose server needed a token could only work if the agent carried the token itself. `ToolResolver` now passes a resolver built from the same `resolveSessionCredentialInjections` boundary the turn uses for sandbox commands, the manager merges the resolved values over the declared `env` (a Vault value wins, because only the operator authorized it), and the merged record is emptied once the process has started so the manager does not hold the value for the session's lifetime. What an MCP tool returns is scrubbed with the same redactor a sandbox tool's return value goes through, resolved per call and cleared immediately, so a server that echoes its credential cannot put it in the transcript. A `limited` credential is refused here exactly as it is for a shell command, since a stdio server declares no host, and the refusal reaches the audit trail. Two published expectations this does not yet satisfy are now recorded where they were previously stated as current behaviour: a `static_bearer` or `mcp_oauth` credential keyed by `mcp_server_url` is still stored and matched but never attached to a url-transport connection, and a rotation still does not reconnect a live transport, so a running server keeps the value it was spawned with.

- Runs webhooks and scheduled deployments without a caller. The delivery engine, the scheduler and the `dispatch` / `retry-due` / `run-due` routes all existed, but nothing projected a durable session event to a subscription and nothing honoured a due schedule unless a caller polled the routes, so an unwatched runtime delivered nothing and never ran a due deployment. The runtime now composes an operations bridge at startup: a broadcast listener projects each event as it is broadcast, and one 60-second tick retries due deliveries and runs due deployments. The tick is `unref`'d so it cannot keep a process alive on its own, the runtime stopper clears it, and a startup re-arm restores the forward schedule of active deployments while a trigger missed while the process was stopped is deliberately not replayed. The routes remain for on-demand passes, and the contract, the capability matrix and `docs/api-matrix.md` no longer describe delivery as caller-driven.

- Renders the runtime's capability state in the Console agent config tab. `GET /v1/x/capabilities` reports, per built-in tool, whether this build can execute it and the reason when it cannot, and `docs/spec/claude-managed-agents-gap.md` requires the Console to render that state rather than hard-coded copy — but the tab rendered a fixed `Tool permissions · Always allow` row. The built-in toolset's rows now come from the registry: only the tools the agent enables are listed, each marked available or unavailable with the runtime's reason carried as its tooltip, and a failed inventory load answers "Capability status unavailable" instead of implying the tools work. The inventory is fetched once per page load and shared, because it describes the runtime process rather than a per-agent resource.

- Renders the agent version diff and restore-as-draft flow in the Console. `GET /v1/agents/{agent_id}/versions` has returned every stored definition for a long time, and `docs/spec/claude-managed-agents-gap.md` lists both a side-by-side diff and a copy-an-old-version-to-draft flow, but the Console had neither: an operator could not see what changed between two saved versions and could not start an edit from an older one. Selecting an agent now offers its stored versions newest first, a field-by-field diff that marks each field `unchanged` / `changed` / `added` / `removed` — comparing tool, skill, MCP-server and metadata lists as lists rather than by identity — and a restore action that opens the edit modal prefilled with that definition as a draft. That modal also runs the local pre-save validation the server would otherwise be the only one to perform, showing the parsed draft and the field-level problems before the save button unlocks; the server still re-validates the full schema on save. No backend change is involved: the endpoint, the version rows and the definition snapshots already existed.

- Opens a webhook signing-secret rotation window. `POST /v1/webhooks` mints one secret per subscription (#318) and the signing format has always accepted a space-separated `webhook-signature` list, but nothing could put a second value in it, so an operator whose endpoint secret leaked had to create a new subscription and move the receiver to a different delivery identity. `POST /v1/webhooks/{webhook_id}/rotate-secret` now mints a new `whsec_` secret, returns it exactly once, and keeps the previous one valid (migration M039): a delivery carries both signatures, newest first, on the first attempt and on every retry, so a receiver can install the new value without missing a delivery. `POST /v1/webhooks/{webhook_id}/retire-secret` closes the window and leaves only the current secret signing, and rotating twice without retiring replaces the window rather than accumulating one. A subscription written before M038 that never held a secret gains one on rotation, which is the call that takes it off the legacy derivation; the Console's retry-due reference entry is corrected in the same change, because it still described a retry as carrying only the legacy header.

- Injects a session's vault credentials into its sandbox commands and redacts what comes back. `resolveSessionCredentialInjections` and `createCredentialRedactor` both existed and were covered by unit tests, but nothing in the executor called them: a session that attached a vault received no injected environment in its commands, and a command that echoed a secret wrote that secret into the event log. A turn now resolves the session's vaults once, passes the environment to the sandbox so `bash` sees it, scrubs every value a sandbox tool hands back, and clears the retained values when the turn ends. A shell command declares no target host, so only a credential the policy admits without one is injected, and a `limited` one is denied exactly as it is for any other hostless call. The delegated child path builds its own sandbox tools and is deliberately not covered: `contracts/anthropic-cma/credentials.md` records that as a boundary, and the capability matrix carries a new `partial` entry naming it rather than leaving it implied.

- Round-trips a credential through the canonical nested wire shape. `src/core/credentials/canonical-credential.ts` implemented the published profile — the nested `auth` object, the `{header, body}` injection location, the locked structural fields — and `tests/unit/canonical-credential.test.ts` covered it with 26 cases, but nothing in `src/` imported it, so the credential routes went on accepting and returning the local flat shape and a client following the contract was refused. `POST /v1/credential-vaults/{id}/credentials` now parses both shapes with that module, refuses a body that supplies both rather than merging them, reports an unexecuted OAuth `refresh` block in a `warnings` array, and every read — list, single read, and the credential returned by rotate and mark-used — carries the canonical `display_name` / `auth` projection. The local flat fields stay on read because a Console page renders and searches on them, so removing them is a Console migration rather than a wire change. `docs/api-matrix.md` also loses a claim it could not support: it recorded a credential update route that does not exist, while the contract has always said structural fields are locked after creation.

- Serves the published credential rotation, use and audit routes. `docs/api.md` documented `POST /v1/credential-vaults/{vault_id}/credentials/{credential_id}/rotate` and `.../mark-used` with a worked example, and `docs/api-matrix.md` recorded a credential audit listing, but the router mounted none of them, so a client following the documented flow got a 404. Rotating replaces the encrypted secret and nothing else — the credential keeps its id, name, policy and history — `mark-used` stamps `last_used_at` and works without a body, and the two audit reads list rotation, use, injection and denial events at vault or credential scope, newest first, without ever returning secret material, surviving the credential's deletion. No migration was needed: `credential_audit_events` has existed since M028. The trail also has one append path now, because the injection boundary's combined row-plus-`last_used_at` write moved onto the same `appendCredentialAuditEvent` the management routes call, so an injection row and a rotation row cannot drift apart. `docs/api-matrix.md` is corrected in the same change: its credential rows spelled the path `/v1/credential_vaults` with an underscore, which is not a route this runtime serves.

- Mints a signing secret per webhook subscription. The `webhooks` table had no secret column and `webhookSecret(deps)` returned the workspace data-directory path, used verbatim as the HMAC key for every endpoint — one key shared by every receiver, and a value that is not a secret. `POST /v1/webhooks` now mints a `whsec_` secret, stores it with the same AES-256-GCM store the credential vaults use (migration M038), and returns it exactly once as `secret_key`; no read path returns it afterwards. Deliveries, retries and the test-delivery route each sign with the endpoint's own secret, and the test route now uses the same key derivation a real delivery uses. A subscription written before M038 holds no secret and keeps the legacy derivation, so an upgrade does not break a receiver that verifies today.

- Serves the published `GET /v1/environments/{id}/work-items` route. `docs/api.md` and `docs/api-matrix.md` had described it, and the integration test for the self-hosted worker keys opened by naming it among the routes it kept real, but no route served the path, so it answered 404 while two documents and a test comment described it as inspectable. The route returns one environment's recent work items — the same shape `/v1/x/worker/claim` hands to a worker — together with that environment's status counts, and `limit` narrows the page with an unusable value falling back to the queue default. An unknown or archived environment is `404`, as its neighbouring worker-key routes are, and a runtime with no work queue configured answers `503 work_queue_unavailable` rather than an empty page, because an unconfigured queue and an idle queue must not read alike.

- Documents the operations namespace in the Console API reference. Webhooks, scheduled deployments and outcomes contribute 25 routes the reference omitted entirely, because the check that keeps the reference and the server in agreement compared a hand-written list of route files that never mentioned `src/api/routes/operations.ts`. That check now expands the real mount graph from `src/api/server.ts`, following composed routers and collapsing the `/v1/x` compatibility mirror onto the canonical `/v1` path only when that path is mounted, so an undocumented route fails it. Two entries describing routes this runtime never had, `POST /v1/scheduled-deployments/{schedule_id}/pause` and `/unpause`, are removed, and the orphan `legacy-providers.json`, which documented the `/v1/x/*-providers` CRUD surface that was removed from the v1 API and was imported by nothing, is deleted.

- Serves the CMA contract matrix from `GET /v1/x/capabilities` under `contract`, beside the existing runtime tool inventory. The matrix records, for every published contract area, whether this build implements the behaviour, the reason for any other status, and the contract document that carries the detail, so the served inventory and the published contract cannot drift apart silently. The addition is additive: `type` and `capabilities` keep their existing shape and status codes.

- Adds path-addressed session memory mounts. A session can mount up to eight stores at whole-segment paths; file tools persist mounted content through `memory_records`, read-only mounts reject writes, shell access is refused while mounts are attached, and existing-file updates require a content precondition.

- Records an immutable version row per memory write. A memory is overwritten in place, so previously nothing could say what a path held before, attribute a change to the session that made it, or notice that two writers raced on the same path. Every create, update, and delete now records the store, the memory, a per-memory monotonic version number, the path, the content, its SHA-256, its size in bytes, the change kind, and the session that made it, and a unique index refuses a second writer claiming a version that already exists rather than letting it overwrite the first. `GET /v1/memory_stores/{id}/memory_versions` lists a store's recorded versions newest first, optionally scoped to one memory, and `GET /v1/memory_stores/{id}/memory_versions/{versionId}` reads one by its own id.
- Attaches a resource to a session as a per-session instance with its own `sesrsc_` id, so it can be listed, read, updated, and detached without rewriting the session payload. Attachment is additive and reversible: detaching a resource from one session leaves it attached to every other session that holds it, and a session can hold more than one resource of the same type at its own position. The session row, its initial events, and its resource instances are written in one transaction, so a failure part-way through leaves neither a session that claims a resource it does not hold nor an orphaned resource row. A `memory_store` resource can only be attached at session creation, because memories are part of the context the session was built with. A github_repository `authorization_token` stays write-only and is never echoed in any response; rotating it is the only updatable field, and changing the repository, checkout, or mount path requires a new instance.
- Governs an MCP tool by its toolset's permission policy instead of letting it fall through to `always_allow`. The toolset kind supplies the default when nothing is configured — `always_ask` for an `mcp_toolset`, `always_allow` for the built-in toolset — and an explicit per-tool or toolset-wide policy overrides it in both directions. A tool the server exposed but the agent never named is governed by the same default rather than by nothing: it is admitted, it keeps no `execute` until the caller approves it, and it is listed for the strategy so the confirmation list and the resolved tool map cannot disagree. A tool the operator marked `never_allow` is not admitted at all rather than shipped and gated, because shipping it invites the model to call something the operator forbade. The runtime naming rule now lives in one dependency-free module so the permission layer reuses it instead of carrying a second copy that used a prefix the runtime never produces.
- Closes the custom tool loop. A custom tool is exposed to the model with its description and input schema and no local `execute`, a call to it is persisted as an `agent.custom_tool_use` event that parks the session in `requires_action`, and the caller answers with `user.custom_tool_result` naming the `custom_tool_use_id` — after which the turn resumes with that result as the model-facing tool result. The runtime never executes a custom tool and never fabricates a result for one. A result naming a call that is not pending, and a second result for a call that already has one, are each refused before they are appended, so a caller cannot inject an answer into a session or answer a call twice.
- Accepts a custom tool either as a canonical `custom` entry in `tools[]` or in the legacy `custom_toolset` grouping, and projects one canonical shape on every agent response. The canonical entry carries `name`, `description`, and `input_schema`, with `parameters` still accepted as a legacy alias. A tool the legacy grouping disables is dropped rather than translated into a policy, and a canonical entry carrying a `permission_policy` is refused because the caller executes the tool and decides whether to run it, so a policy field would claim governance the runtime does not hold. A name that collides with a built-in tool, a name declared twice across both shapes, and a malformed input schema are each refused with a message naming the offending entry. `getCustomToolNames`, `getCustomToolConfigs`, `canonicalToConfig`, and `findCustomToolConfig` report the same tool set for both wire shapes.
- Publishes stable admission error codes on rejected compatibility requests.
  `error.code` now names the cause (`missing_anthropic_version`,
  `unsupported_anthropic_version`, `missing_anthropic_beta`,
  `malformed_anthropic_beta`, `unsupported_anthropic_beta`,
  `conflicting_memory_store_beta`) so a client can branch on the reason
  instead of matching the message text. The `/v1/x` extension root and its
  subtree remain outside CMA admission.
- Cross-checks an agent definition's `mcp_servers` against the `mcp_toolset`
  entries that bind it. A toolset naming an undeclared server, a declared server
  no toolset references, and a duplicate server name are each refused with a
  message naming the offending entry. Previously all three saved successfully and
  then silently did nothing at execution time. Two toolsets may still bind the same
  declared server.
- Evaluates a scheduled deployment's cron expression in the deployment's own
  timezone instead of UTC. The same wall time resolves to a different instant in
  a different zone, and the difference moves across the year for a zone that
  observes DST. A wall time inside a spring-forward gap yields no run rather than
  a silently shifted one, a fall-back overlap resolves to one deterministic
  instant, and an unknown IANA zone name is refused rather than defaulted to UTC.
  `nextCronRun` now takes an optional zone, defaulting to UTC so an existing
  caller is unchanged.
- Signs webhook deliveries with the Standard Webhooks v1 headers. A delivery now
  carries `webhook-id`, `webhook-timestamp`, and `webhook-signature`, with the
  signature covering `id.timestamp.body` so a receiver can detect a replayed or
  altered delivery rather than only a forged one. Verification is constant-time
  and accepts several space-separated signatures in one header, which is the
  rotation window. A `whsec_`-prefixed secret is base64-decoded to key bytes and
  an unprefixed secret is used as raw UTF-8, and the legacy
  `X-Managed-Agents-Signature` header is still sent.
- Adds `POST`, `GET`, and revoke routes for a self-hosted environment's worker
  keys. Only the SHA-256 hash is stored, so `secret_key` is returned exactly once
  and list and revoke responses carry `key_prefix` instead. A claim on
  `POST /v1/x/worker/claim` may present the key as `environment_key`, which scopes
  the worker to the issuing environment; a claim naming a different environment,
  or presenting a revoked, expired, or unknown key, is refused before any work item
  changes hands. The issuing side and the consuming side now share one definition
  of the hash scheme and the status rules.
- Enforces the published memory rules on the routes that own them: content is
  capped at 100 kB measured in bytes rather than characters, a store holds at most
  10,000 memories, a store's session-level `instructions` field is capped at 4,096
  characters, and `GET /v1/memory_stores/{id}/memories` accepts `path_prefix` and
  `depth` with segment-based prefix matching. A write may carry a
  `content_sha256` precondition; a stale one is refused with `precondition_failed`
  and the current hash, and an unknown type or a missing hash is refused with
  `invalid_precondition` rather than ignored.
- Executes `web_fetch` behind an SSRF address guard. The guard refuses internal
  host names before DNS, refuses every resolution that is loopback, RFC 1918,
  link-local, or CGNAT (including IPv4-mapped IPv6 forms), and pins the connection
  to the validated address so a second resolution cannot disagree with the first.
  Every redirect hop restarts the checks, the domain lists the agent declared are
  enforced, and an oversized body, a stalled response, and a binary content type
  are each handled without inlining the content. Failures return a tool error
  rather than a fake success. `web_search` stays unavailable because no search
  provider is bundled.
- Publishes the files an agent writes under `/mnt/session/outputs/` as
  session-scoped file records after each turn, so the deliverables become
  retrievable through the Files API. The output directory is walked rather than
  reported through a side channel, recording is idempotent per (session, sandbox
  path) so a deliverable keeps one file id across passes, the number of new files
  per pass is capped, and a collection failure is swallowed because the turn has
  already completed.
- Sends the canonical Anthropic compatibility header pair from the first-party
  SDK on every `/v1/...` request: `anthropic-version: 2023-06-01` plus
  `managed-agents-2026-04-01`, or `agent-memory-2026-07-22` for a memory-store
  path. `/v1/x/...` receives no compatibility header, and a caller-supplied beta
  overrides the derived one. The three literals now have one definition shared by
  the admission middleware and the SDK, so the two cannot drift apart.
- Accepts `system.message` as an inbound session event alongside the `user.*`
  family. The payload uses the same content-block vocabulary as `user.message` and
  must be a non-empty array of at most 1000 valid blocks; an over-long batch is
  reported as a size violation naming the ceiling rather than as a generic shape
  error. The event projects as its own `system` role turn, so it applies to the
  accompanying turn and every later turn rather than folding into the agent's
  top-level prompt. A message that arrives while an assistant turn is pending
  flushes that turn first, and one with no usable text is dropped instead of
  projected as an empty turn.
- Spills an oversized tool result into the sandbox and gives the model a short
  preview plus the path it can read the full content back from, through one
  contract shared by the built-in tool path, the MCP tool path, and the Pi stdout
  translator. The spill path is recorded on the `agent.tool_result` event only
  when a file was actually written, and a failed spill degrades to a path-less
  preview instead of failing the turn. The local ceiling stays at 50,000
  characters rather than the published 100,000, because a local runtime persists
  every event into SQLite.
- Lets a session event stream opt into token-level previews with a repeated
  `event_deltas[]` query parameter for `agent.message` and `agent.thinking`. An
  unsupported value, an empty value, or more than 100 values is rejected with
  `invalid_request` before the stream opens. Preview frames carry `event_start`
  and `event_delta`, never carry an `id` or `processed_at`, and are never
  persisted, so they cannot advance the resume cursor. `agent.thinking` gets an
  `event_start` only, because the buffered event carries no reasoning text and a
  delta would have to invent content.

- Adds `POST /v1/runs`, which starts one turn and returns its result without
  driving the session lifecycle. `response_mode` selects `wait` (200 with
  the output and usage), `sse` (event stream), or `async` (202 with a
  query handle); `max_wait_seconds` bounds only the wait and answers 202 with
  `wait_deadline_reached` when it elapses, leaving the turn running. A refusal
  before a turn starts answers with its own status, and a failure raised while
  waiting or streaming is recorded once as `session.error` so it replays from
  the session's event log. Session budgets are not part of this endpoint.

- Lets `POST /v1/sessions` start a session in one call with an optional
  `initial_events` array of `user.message` events. A non-empty list yields a
  `running` session whose event log already holds every supplied event, in
  order; an absent field and an empty array still create an idle session. The
  batch is validated and written inside the creation transaction, so a rejected
  batch leaves no session row and no partial history behind.
- Attributes `agent.mcp_tool_use` and `agent.mcp_tool_result` events to the MCP
  server that produced them. Both events carry `mcp_server_name`, results also
  carry `mcp_tool_use_id`, and the identity is projected onto the public API
  event, so two servers exposing the same tool name stay distinguishable in the
  durable log.
- Writes a `session.usage` snapshot event immediately before every
  `session.status_idle`. The snapshot reports the session's aggregate
  input/output token counters plus `active_seconds`, the wall-clock time the
  harness loop spent executing the session, derived from the append-only event
  log so it survives restarts without a migration. Cost, budget, and
  server-tool counters are omitted rather than reported as zero.

- Makes new-session `loop_engine` admission fail closed. The optional request
  override resolves against one Settings descriptor source, freezes the selected
  executable engine on the session, returns stable `loop_engine_invalid` and
  `loop_engine_not_supported` errors before persistence, and documents that Pi
  native tools are outside Harness approval and sandbox path policy.

- Exposes `GET /v1/x/capabilities` as the canonical inventory for locally
  executable built-in tools. Agent create/update and session creation now
  reject enabled `web_fetch` and `web_search` requests with a structured
  `unsupported_capability` error before persistence or model/tool execution.

- Prices model consumption and enforces a session spending ceiling. `POST /v1/sessions` accepts an optional `budget` (`{type: "limit", max_list_cost: {amount, currency: "USD"}}`, where `amount` is an integer number of cents written as a string), it is stored in a new nullable `sessions.budget` column, and the session echoes it back. Cost is computed from the session's own `span.model_request_end` records into integer microcents, so the ceiling comparison never depends on float rounding, and the reported `list_cost` is that total rounded up so a client is never told it spent less than it did. Once the spend reaches the ceiling, the next event that would start model work is refused with `budget_reached` and only events that settle work already in flight are accepted, so the next model request does not start. Prices come from an operator-supplied profile (`MANAGED_AGENTS_COST_PROFILE`) rather than vendor list prices: a model with no entry has no list price, a budgeted session naming one is refused with `budget_model_without_list_price` rather than metered against an invented rate, and `usage.list_cost` is omitted while any used model is unpriced instead of being reported as a partial total. `session.usage` now also carries the session's `budget` (or `null`) and zeroed `server_tool_use` counters. A budget is attachable at creation only, cannot be raised to a value at or below what the session already consumed, and cannot be re-added after removal; `session.usage`/the session payload keep "never had a budget" and "had one removed" distinguishable. The capability matrix entry moves from `planned` to `partial`.

- Exports a session as a replayable handoff bundle. `POST /v1/x/sessions/{id}/handoff-bundle` packages one session as recorded evidence and stores it, `GET /v1/x/handoff-bundles` lists stored bundles newest first, and `GET /v1/x/handoff-bundles/{id}` returns a stored payload unchanged. The package uses three open standards rather than bespoke formats: RO-Crate 1.1 for the description graph, BagIt (RFC 8493) for sha512 integrity manifests with a manifest self-digest, and an in-toto statement in a DSSE envelope signed with an Ed25519 key derived from the runtime secret key material — a local trust root that proves origin and integrity, not authorship. Message bodies and file bytes are excluded by default, and each excluded body is replaced by a byte count and sha256 reference, so prompt text and file contents leave the runtime only when the caller asks for them. Credential secrets never enter a bundle: denied and injected credentials are recorded as metadata only, and any transcript key naming a secret is replaced with `[redacted]` and reported in `redaction.scrubbed_fields`. `replay.mode` is `recorded_replay`, with tools never re-executed and model requests never re-issued, while `resume` and `fresh_run` are declared unsupported rather than implied. Bundles are stored immutably in a new `handoff_bundles` table (migration M036).

- Accepts, validates and persists a scheduled deployment's `timezone`. The field was documented and the cron engine already evaluated an expression in a zone, but nothing could set one: the request body was not read, no column held it, and every schedule therefore ran in UTC. Both `POST` and `PUT /v1/scheduled-deployments` now read the cadence from either the flat `cron` and `timezone` fields or the canonical `schedule: { type: "cron", expression, timezone }` object, refuse an unknown IANA name with `400` without writing a row, default to `UTC` when the field is omitted, and echo the stored zone on every response. `next_run_at` is computed in the resolved zone, and an update that moves the cadence re-arms it in the new zone unless the caller supplies its own `next_run_at`. The column is added by migration M037 as `NOT NULL DEFAULT 'UTC'`, so a schedule written before it existed keeps running on the same instant it did.

### Changed
- An interrupted tool call now reports that its external outcome is **unknown** instead of suggesting a retry. When the process dies mid-turn, or a turn fails before the tool returned, the runtime appends a placeholder `tool_result` so the message sequence stays paired. That placeholder read `(interrupted by server restart — retry if needed)`, which is an instruction to do the one thing that must not be done: for a call that already wrote a file, sent an HTTP request or ran a shell command, it asserts the effect probably did not happen and invites a second one. The runtime knows exactly one thing in this situation — that no `tool_result` was recorded — and cannot know whether the call took effect, so the message now says the outcome is unknown (`Tool call was interrupted before its result was recorded; its external outcome is unknown. It may or may not have taken effect.`). The text is one exported constant (`INTERRUPTED_TOOL_OUTCOME_MESSAGE` in `src/core/session/session-recovery.ts`), beside the function that defines what an orphan is. The two call sites previously carried **different sentences for the same epistemic situation** — the crash path the one above, and the failed-session resume path `(previous turn failed before this tool returned)` — so the message stopped being a parameter: both paths now record the constant, which is what keeps them from drifting apart again. The record is still `is_error: true` and still pairs with its `tool_use`; what changed is the claim it makes. Tool calls are still not retried, and a crashed *model* call is still not recovered — saying the outcome is unknown is what makes a later retry decision correct rather than blind, so classification is deliberately a separate change. Only runtime-executed tools reach this path (`OrphanedToolUse.resultType` covers `agent.tool_result` and `agent.mcp_tool_result`), so the statement is always about a call that was actually dispatched — a custom tool is executed by the client and is never described this way.
- Publishes `deployment_run.started`, `deployment_run.succeeded` and `deployment_run.failed`, so a subscription learns that a timed run happened and how it ended. All three were accepted in a subscription and could never fire, so a receiver could not tell "no run has come due yet" from "this runtime has no run events". The published table describes them as one mechanism, not three: `.succeeded` and `.failed` carry `data.id`, the **run** id, and it states that this is the same id as that run's `.started` event — so the id linkage is an invariant spanning all three names, and implementing a subset would ship a correlation the contract promises and the runtime could not honour. That is why they are one change. The run id is `data.id`, the id of the `scheduled_deployment_runs` row, and therefore the id `GET /v1/deployment_runs/{id}` accepts. `succeeded` is raised for a row with status `created_session` and `failed` for `failed`, exclusively — a failed run never also reports success. **Only a timed run raises them.** The manual route `POST /v1/deployments/{id}/run` shares `runSchedule` with the timed path, and it takes a **caller-supplied** `trigger_type` (`stringField(body.value.trigger_type) ?? 'manual'`), so the published rule could not be written as `triggerType === 'scheduled'`: a manual run would have declared itself timed and emitted into a rule that excludes it. The rule lives on the path instead — publication is in `runDueScheduledDeployments`, which only the background tick and `POST /v1/deployments/run-due` reach — so a manual run publishes nothing whatever it calls itself. `deployment_run.started` is published once the run is **recorded**, before its outcome rather than at the instant it begins: `runSchedule` is synchronous and writes its row in a single terminal statement, so there is no in-progress state for `started` to point at, and the published handler contract tells a receiver to fetch the resource by `data.id` — publishing earlier would send that fetch to a `404` for a run that had genuinely started. The event is late rather than false, and a test measures resolvability **inside the receiver** so the claim is checked rather than asserted; adding a persisted in-progress run state is a change to the run status vocabulary, not to this event, and is not part of this change. Delivery stays best-effort: publication is attempted per run, and a subscriber that cannot be reached never stops a due deployment from running or abandons the rest of the pass — asserted with two due runs and a subscriber on a refusing port, which also checks the failed attempts are recorded for retry. `deployment.deleted` remains unemitted: there is no `DELETE` route for a deployment in any API route module.

- Publishes `deployment.archived`, so the route that ends a deployment's life stops being silent. `POST /v1/deployments/{id}/archive` marked the row archived and returned it while subscribers heard nothing — and this is the event a receiver cannot afford to miss, because missing it means continuing to track a schedule that will never run again. The published row gives it two causes (`订阅Webhook.md:62`): a direct archive, or the deployment's agent having been archived. Only the **direct** cause is implemented here. The second does not exist — `POST /v1/agents/{id}/archive` (`src/api/routes/agents.ts:164`) updates only the `agents` table, and `src/core/operations/scheduler.ts` reads deployments and re-arms `next_run_at` / `last_run_at` without ever archiving one — and the rule it depends on has no representation either: the published text says a deployment with no schedule is never auto-archived, but `cron` is `NOT NULL` and every create arms `next_run_at`, so "no schedule" is not a state a deployment can be in. That is a behaviour of the scheduler and the agent route with its own prerequisite, so it is recorded in `operations.md` §4 as a gap rather than half-built; a test asserts the cascade's **absence** so that implementing it later has to update the expectation deliberately. The event is published only when the archive actually happened, and **after** the write, so a receiver that resolves the reference at delivery time sees `archived_at` set rather than an unarchived deployment — measured inside the receiver, since a read after the route returns would see it either way. A repeat archive publishes nothing and answers `404`: `archiveById` filters `archived_at IS NULL`, so the second call is not an archive, and the published table states the no-op rule for the sibling resource ("对已归档的环境再次归档不会发出任何事件", `:79`). That case asserts **both** halves — the `404` and the silence — because either alone passes for the wrong reason: a route that never publishes satisfies the silence, and a route that publishes on every call satisfies the `404`. To make this possible the shared `archiveById` helper now reports **what it did** alongside the response it already built, rather than the deployment route copying the `archived_at IS NULL` guard. Three resources share that helper, and only a deployment publishes, so teaching it about deployment webhooks would have been the wrong seam; a second copy of the guard is how two routes come to disagree about when an archive happened. The response is still constructed in the helper and returned unchanged, so the webhook and outcome routes are behaviourally identical — asserted, not assumed, because that is exactly the kind of refactor that changes a caller nobody looked at. `deployment.deleted` remains unemitted: there is no `DELETE` route for a deployment in any API route module, so there is nothing to carry it.

- Publishes `deployment.created`, so a subscription learns a deployment exists — the one thing about it a receiver cannot reconstruct. `POST /v1/deployments` inserted the row, returned `201`, and told nobody. `deployment.updated` (the preceding change) reports changes to something that already existed, so a subscriber that started after the create but before the first update had no way to learn the deployment was there at all: the published table lists `deployment.created` (`订阅Webhook.md:58`) and the runtime produced none of the four `deployment.*` lifecycle names. The event is published **after** the insert, so a receiver that resolves the reference when the event arrives finds the deployment rather than a 404 — and the test measures that ordering **inside the receiver**, by asking the database at delivery time, because a read after the create returns would find the row whichever way round the handler did it. A create that is refused — missing `name`, missing `agent_id`, a schedule with the wrong field count, an invalid time zone, a body that is not an object — fails before the row exists and publishes nothing: there is no id to name, and an event pointing at a row that was never inserted would send a receiver to a 404. A deployment **created already `paused`** publishes only this event, not `deployment.paused`. The pause events report a *transition*, and nothing moved here — there was no unpaused state before it — so publishing one would assert a transition that did not happen, which is the same class of mistake as the no-op events the pause work rules out. Nothing is lost: `deployment.created` names the resource and the published contract says a webhook body is a reference the receiver resolves by `GET`, so the receiver learns the status by fetching it, which is the mechanism the published contract supplies for exactly this. `deployment.archived` was subsequently published for its direct cause by the change above, which took the shared-helper decision noted here; its agent-archived cause and `deployment.deleted` remain unemitted.

- Publishes `deployment.updated`, the last name in the deployment write family with no emitter. `PUT /v1/deployments/{id}` rewrote eight caller-visible fields, returned the updated object, and told nobody — and it is the most commonly called route of the three, because changing a schedule is the ordinary reason to reach for it. The published table gives the event its own trigger ("部署属性已更改（例如，其计划已更新）", `订阅Webhook.md:59`), and the nearest published rule for what counts as a change is stated for the sibling resource: "环境已更新，且至少有一个字段发生了变化。**无操作的更新不会发出任何事件**" (`:78`). That second half is the whole difficulty. Every field `PUT` writes is now compared against the row read before the write, and the event fires only if at least one differs: `name`, `agent_id`, `environment_id`, `cron`, `timezone`, `next_run_at`, `payload`, `metadata`. Two things are deliberately not counted. `status` and `paused_reason` belong to the pause transition, which has dedicated events and which the two pause routes also perform — if this event covered them, `POST /{id}/pause` would have to publish `deployment.updated` too, contradicting the published design of a dedicated event for that transition. `updated_at` moves on every write by construction, so counting it would make the no-op rule unreachable and every `PUT` would publish. A `PUT` that changes both a field and the pause state publishes both events, one per change. The stored `payload` and `metadata` are **serializations**, so the comparison has to be structural: `JSON.stringify` preserves insertion order, which means a text comparison reports a change for the same object re-sent with its keys in another order — a change the caller did not make and cannot avoid, since they do not know the order the server wrote. `equalJsonObject`/`equalJsonValue` in `src/api/routes/operation-helpers.ts` compare parsed values with keys sorted at every level, and a nested case is asserted alongside the key-order one. Resolving every written value into a local before the write is what lets the same values decide both what to store and whether anything changed; reading them twice is how a comparison comes to describe a different write than the one that happened. `deployment.created`, `deployment.archived` and `deployment.deleted` remain unemitted, and are recorded as gaps rather than implied to work.

- Publishes the pause event an update causes, so `deployment.paused` and `deployment.unpaused` follow the state change rather than the route. The preceding change wired up `POST /v1/deployments/{id}/pause` and `/unpause`, and its entry claims that "pausing a deployment publishes `deployment.paused`" — which was true of one route and **false of another that produces the same durable state**. The pause state has a third door: `PUT /v1/deployments/{id}` writes `status` and `paused_reason` straight from the request body, so `PUT {"status": "paused"}` paused a deployment and told no subscriber. A receiver watching for pauses still missed them, and the runtime's own documentation was the thing that was wrong. The real defect was the shape of the rule, not a missing call: "did the pause state change, and which event does that mean?" was written twice as two local booleans in the two pause handlers, so adding a third copy to the update route would have made three — and three copies of a rule that must agree is how they stop agreeing, which is the same failure the shared signing-key derivation was extracted to prevent. The decision now lives in `publishPauseTransition(deps, id, previous, next)` in `src/api/routes/operation-events.ts`, and all three doors call it with the state they read before writing and the state they wrote. That makes the rule impossible to omit: a route can only pass the wrong pair of values, not forget the comparison. The two pause routes were rewritten onto the helper, so their behaviour is asserted again in a new file rather than inferred from the neighbours being green, including a mixed sequence that pauses through the update route and resumes through the pause route and then the reverse — the events are derived from the stored state, so a route holding its own idea of the state would publish a duplicate or nothing. Nothing is published when the state does not move, which now holds at all three doors: a `PUT` re-sending the status already stored, a `PUT` that changes an unrelated field, and a `PUT` on a paused deployment that leaves it paused are all silent. `deployment.updated` is **still not emitted** and is recorded as a gap in `operations.md` §4 — a rename publishes nothing at all, and giving it a trigger means comparing every written field against its stored value to satisfy the published no-op rule for update events.

- Scopes the file listing by the published `scope_id` instead of ignoring it. `GET /v1/files?scope_id=<session_id>` is the call the published contract documents for an agent's deliverables — it is the first line of `tests/unit/session-outputs.test.ts`'s own header — and the handler read no query parameters at all, so it answered with **every** file in the workspace. The response was indistinguishable from a session-scoped listing that happened to contain files the caller did not recognise, so a caller asking for one session's outputs had no way to tell they had been handed the global list. Before this change, `scope_id` was pure decoration: the runtime collected session outputs into the store and recorded which session each belonged to, but the read path that was supposed to filter by it never looked. The filter is applied in the query, so a `scope_id` naming no session returns an **empty page** rather than falling back to the global listing — the fallback is precisely the failure this closes, and "which files does this session have" is answered truthfully by an empty page. Omitting `scope_id` returns the same full listing as before, so the Console and the SDK, which call this route without a scope, are unaffected; a file uploaded through `POST /v1/files` records no session and therefore appears in the full listing and in no scoped one, rather than being attributed to a session that did not create it. Because the route now reads a query parameter it is also covered by the refusal for unimplemented parameters, so `?include_archived=true` is answered by name instead of ignored. No migration: `files.session_id` already existed and was already populated by the output collector — this was a read-path gap, not a schema one.

- Refuses a query parameter a route does not implement, instead of ignoring it. A dropped filter is worse than an error, because the answer is plausible: `GET /v1/sessions?include_archived=true` answered `200` with the same list it would have returned without the parameter, and `GET /v1/deployment_runs?deployment_id=nope` answered `200` with an empty page that is indistinguishable from "this deployment has no runs" and from "you misspelled the parameter". Neither response invited the caller to look again. This is the rule `contracts/anthropic-cma/agents.md` §2 already states for request fields — "Unknown keys and unsupported field values are rejected, not silently discarded" — applied to the query string, where it had no equivalent, and the message names the offending parameter and lists the ones that route accepts so the caller can fix the call rather than guess. The allow-list is passed by the handler that reads the parameters rather than kept in a table in the admission module: a central route-to-parameters table is a second description of the same fact and the kind that drifts, leaving a parameter a handler does read refused by a list maintained somewhere else. The refusal is decided before the resource is looked up, so a request that is wrong in both ways reports the parameter rather than a `404` that would imply it was understood. `beta` is accepted on every route and **ignored**, because 36 published examples put it on the URL (`?beta=true`, and `?beta=true&deployment_id=...`) rather than in a header, so refusing it would break a client built against the published documentation; it is deliberately absent from the advertised list, since that list names parameters that do something and accepting `beta` must not be read as honouring it. Routes that read no query parameters are **not** covered, and that gap had teeth in one place: the published call `GET /v1/files?scope_id=<session_id>` reached a handler that reads no parameters at all, so `scope_id` was silently ignored and the caller received the unscoped list — closed immediately above, which is why `files.ts` now appears in this list too.

- Answers every invalid request with one error type. The runtime used two spellings for the same category — `invalid_request` on most routes and `invalid_request_error` where a route had been written later — so a client branching on `error.type` had to know which family it was talking to in order to compare one category, and an SDK generated from the published contract (which names this category `invalid_request_error`) had to special-case the difference. The canonical spelling is now `invalid_request_error`, emitted from the shared `resource-utils.ts` helper, its twin in `operation-helpers.ts`, the admission middleware, and every hand-written envelope in the route modules. `invalid_request` remains a **valid alias** for the same category rather than a silent break: a client may treat the two as equal, and the alias is recorded in `errors.md` §4 with its expiry — the official SDK conformance suite passing against the canonical value — because a permanent second spelling would be a second contract. `not_found` was deliberately **not** renamed: no canonical counterpart for it appears in the published material, and the comment that claimed `not_found_error` was the canonical form was asserting a value that appears nowhere, so it was corrected rather than followed. Two things this touched are worth naming. The session resource routes were the one place where the wire type was not a literal but a core `code` passed straight through (`type: result.code`), so canonicalising route literals alone would have left them emitting whatever that internal vocabulary said; the route now translates through an explicit `WIRE_ERROR_TYPE` map. And that translation's `invalid_request` arm is **not reachable over HTTP today** — the add and patch handlers refuse those cases with their own literal before calling into core, and session creation stores `params.resources` as a JSON column without materialising instance rows, so no memory_store instance exists to delete. It is a correctness fix for a coupling that would emit the legacy value the moment any of those branches becomes reachable, and the guard is what keeps the literal from coming back. `tests/integration/invalid-request-spelling.test.ts` takes a rejection from each mechanism — the shared helper, the operations twin, the admission middleware, a route literal, the stream query validation, and the core-code translation — and adds a source scan, because the spelling is a literal in fifteen modules and a route added later is one `type:` away from restoring the split.

- Refuses a `multiagent` roster on both agent write paths instead of dropping it on one. The create schema is not strict, so `POST /v1/agents` accepted a canonical roster, stripped it, and answered `201` for a definition that delegated to nobody, while `PUT` and `PATCH` rejected the same field as unknown — one request, two answers, and the permissive one was wrong. Both paths now answer `400` naming the capability `multiagent-roster` and pointing at the local extension, and the check reads the caller's own payload rather than the parsed copy, because a schema that strips the key is exactly what makes a stripped roster invisible.
- Accepts the local `enable_general_subagent` extension on both agent write paths. Create took the field, but `UPDATE_FIELD_SCHEMAS` did not declare it, so an update that only toggled the flag was refused as an unknown field even though the definition it read back held one. `DelegationService` builds the `general_subagent` tool from that field, so refusing it made the two write paths disagree about a field the runtime acts on. The capability matrix now registers the local single-level `delegations` / `enable_general_subagent` extension as its own `supported` entry rather than letting the canonical roster stand in for it.
- Makes the contract surface check itself instead of reading as green. The capability matrix, the prose under `contracts/anthropic-cma/`, the source and test files those contracts name, and the routes the server actually mounts are four descriptions of one build, and nothing compared them: a contract could credit a real test file with covering wiring that file never touched, and a matrix entry could report `supported` for an implementation no runtime composition injects. `tests/unit/contract-honesty.test.ts` now runs the comparison — every entry's `contract` path exists and no `id` repeats; every contract file restates its entries' statuses in a machine-readable `<!-- capability-status -->` block that the guard holds equal to the matrix in both directions; every backticked `src/` and `tests/` path a contract cites exists; a status claiming an implementation cites an existing source file and `supported` additionally cites an existing test; a capability whose only path to a caller is the composition root must actually be wired there; and the route table in the new `contracts/anthropic-cma/routes.md` is compared against `mountedRoutes()` by method **and** path, so a documented route answering a different verb fails as loudly as a route that is missing. Every rule is also driven with a fixture that must fail, so the guard cannot pass by finding nothing. The drifts it found are corrected rather than recorded as current behaviour: `github-repository-materialization` and `file-resources` drop from `supported` to `partial` (the materializer and the file mount are implemented and tested, but no composition supplies either, so a session that declares one is accepted and then fails on its first turn), `threads-and-coordinator` and `dreams` become `unavailable` (`partial` and `not_applicable` understated a surface that does not exist and a capability that belongs in a local-first runtime), `github-repository.md` no longer claims runtime wiring it does not have, `tools.md` no longer reports `web_fetch` as unavailable, `credentials.md` §4 leads with the plaintext-injection deviation and the missing egress substitution, `sessions.md` and the matrix agree that the outcome revision loop is `supported`, and `pagination.md`, `events.md`, `operations.md` and `files.md` cite tests that exist.
- Binds a Pi resume to the contract the session's earlier turns ran under instead of to the existence of its session file. A managed session file proves which Pi conversation is being continued and nothing about what it was continued under, so `pi_session_state` now records the work directory those turns ran in and a fingerprint of the compiled tool plan (allow, gated, denied, and whether anything is exposed at all), the model and provider, and the approval mode (migration M041, two nullable columns). A launch compares both against what the row recorded *before* it acquires a child, and a resume whose directory or fingerprint differs is refused with the stable code `pi_policy_mismatch` — naming which of the two changed, rather than reporting the generic `pi_session_discontinuous` a caller cannot act on — and records the refusal durably so the next attempt reads it instead of rediscovering it and cannot fall through to a second Pi history. The binding is computed once per turn and used for both halves of continuity, so the value the launch proves is the value the settled turn records; it is re-recorded on every settled turn, and the column keeps a value a later write does not restate, so a recorded contract is never erased by a turn that only knows the identity. Re-ordering an equivalent compiled policy is not a change: the fingerprint sorts the tool sets before hashing. Nothing is refused for being unrecorded, because a row written before M041 has no value to compare against, so a session inherited by an upgrade still resumes; the print-mode launch, which carries no compiled policy, proves the half it can state (the work directory). No turn protocol, compiled flag, gate path, or approval rule changed.
- Stops an `always_ask` native Pi tool call before it executes instead of refusing the agent that declares one. `pi_always_ask_not_supported` is retired as a behaviour — no code path raises it, and an agent whose effective policy marks a native tool `always_ask` is now admitted — so the refusal that used to stand in for a gate is replaced by the gate itself. A launch whose compiled plan names a gated tool materializes a SandBase-owned `gate-extension.mjs` into the session's private Pi configuration directory, passes it with `--extension` beside `--no-extensions` so project-local content cannot add or replace it, and hands the gated tool names and session id to the extension through its own environment. The extension's `tool_call` hook blocks the call and asks through an `editor` dialog carrying a `sandbase_tool_gate` payload (the tool call id, the tool name, and the input a decision is being made against), and the adapter proves the gate loaded before exposing a gated tool at all by asking for Pi's command list and requiring the per-session marker command the extension registers (`pi_rpc_gate_unavailable` otherwise). Each request is recorded durably in `pi_tool_interactions` before the caller is told anything, the session publishes the `agent.tool_use` with `requires_confirmation: true`, the confirmation group and the input fingerprint, and the session reports `requires_action` while staying busy, because Pi is suspended inside its hook and the same turn continues when a decision arrives. A decision is a `user.tool_confirmation` naming that `tool_use_id` (Pi's dialog request id selects the same gate), which `assertPiUserEventCanExecute` now admits, and it is consumed exactly once by a conditional update whose affected-row count is the proof: a duplicate, a decision for a turn that has moved on, a decision naming a different tool, or a decision that cannot be recorded finds no pending row, is answered with a denial, and never executes the call. Replacement arguments are re-validated before they are written back, because Pi re-validates nothing after an extension mutates `event.input`, and a malformed replacement denies the call. Every path that cannot produce a trustworthy decision denies: an unrecognized payload, a tool this session does not gate, no turn in flight, a second gate while one is pending, a malformed reply, a turn past its deadline, a transport that closes while a decision is pending, and a session composed without the durable store. The executor no longer routes a Pi `user.tool_confirmation` through the builtin `ToolResolver`, so a Pi native tool is never executed in the Harness sandbox while Pi waits for a decision it will not receive. `docs/api.md` and `docs/pi-loop-engine.md` state the boundary the gate does not move — Pi native tools are still not Harness `ToolResolver` tools and still receive no sandbox path confinement — and the three gate codes (`pi_rpc_gate_unavailable`, `pi_rpc_gate_lost`, `pi_rpc_approval_not_pending`) are classified `not_retryable`.
- Runs Pi as one session-owned RPC child for the session's whole life instead of one print-mode process per turn. The first turn of a session starts the child (`--mode rpc`); every later turn writes to the same child, so the conversation no longer depends on Pi reloading it from disk between turns, and a second turn cannot be handed to a child that is still serving the first. Every frame the runtime writes goes through the session owner and its one serialized writer, and the session owner — not the strategy — now owns the turn protocol, so a turn that settles, fails, or runs past its deadline settles as the outcome the runtime already publishes (`pi_timed_out`, session status `timed_out`) rather than as a promise nobody resolves. A turn whose deadline passes cancels the child, so the next turn cannot inherit a wedged engine, and a child that exits or whose transport dies fails the turn with the reason Pi gave (a resume refusal stays distinguishable from a crash) and refuses every later write to it. The child and its session-file lease are released on close, stop and delete, before the sandbox work directory those turns used is torn down. The turn protocol is stated once in an internal `LoopEngineAdapter` contract (`src/strategy/loop-engine/adapter.ts`) that the Pi strategy consumes, so a second engine can be added without the strategy learning its wire protocol. The compiled tool flags, the CLI/`PATH` requirement, the managed session file and its continuity state, and the `pi`/`builtin` capability boundary are unchanged; Pi still ships no security sandbox, and its native tools are still not Harness `ToolResolver` tools.
- Enforces a Pi agent's declared tool policy in the child instead of refusing the agent for declaring one. The compiled flags now reach the launch (`--tools` with the enabled set, `--exclude-tools` with the tools the policy denies, `--no-builtin-tools` instead of an empty allowlist), so an agent whose tools include a `never_allow` or `enabled: false` entry runs with that tool genuinely unavailable rather than being refused outright, and the same flags admission compiled are the ones the child receives. A request that states no tool policy at all is launched with `--no-builtin-tools`, because the safe default is expressible: a launch that cannot say which tools are allowed exposes none instead of inheriting Pi's full toolset. `always_ask` still reports `pi_always_ask_not_supported`, since the pre-execution gate that would ask is its own change.
- Compiles a Pi agent's native tool policy instead of refusing every agent whose policy is not the default, and names what it refused. Pi's native tools are not Harness `ToolResolver` tools, so the only way to make a declared policy true is to express it in Pi's vocabulary; the runtime now derives that expression (`--tools` for the enabled set, `--exclude-tools` for a tool denied by `never_allow` or `enabled: false`, `--no-builtin-tools` when nothing is left) from the *effective* policy helpers, so a toolset-level `default_config` cannot be missed by reading only per-tool entries. `pi_tool_policy_not_supported` now carries a message naming the declaration responsible — the tool Pi does not have, or the `mcp_toolset` Pi has no transport for — because "this agent cannot run on Pi" is only actionable once the operator knows which entry caused it. One over-broad rule is corrected rather than preserved: a fully disabled `mcp_toolset` is admitted, since nothing is expected to run through it and there is nothing to enforce, which makes no tool available. The last piece is deliberately not claimed: those flags are not sent to the child yet, so an agent declaring a denied or disabled native tool is still refused instead of being launched with a policy nothing enforces, and `always_ask` still reports `pi_always_ask_not_supported`.
- Carries a structured `{ type, message, retry_status }` payload in a top-level
  `error` field on every `session.error`. `type` is the stable code the runtime
  attached, or `internal_error` when the failure carries none, and `retry_status`
  is derived from that code rather than guessed: `pi_session_busy` is `retryable`,
  the Pi admission codes, the loop-engine codes, `pi_timed_out`,
  `pi_cleanup_pending`, and `unsupported_capability` are `not_retryable`, and any
  other code is `unknown`. An aborted turn still records no `session.error`.
- Treats a session file resource's `mount_path` as a logical path inside the
  session instead of an internal sandbox path. `/data.csv` is accepted and maps
  under the runtime's own mount root, the full relative path is preserved rather
  than flattened to its basename, an omitted or blank path defaults to the file
  id, and traversal-shaped paths are still rejected. The historical `/uploads/`
  spelling remains accepted as a logical path.
- Accepts the canonical agent `model` object form and carries `effort` through
  to the stored agent instead of dropping it. `speed` keeps its `standard`
  default and also accepts the `extended` local value. An unrecognized key and a
  well-formed `inference_geo` pin are both refused with
  `unsupported_model_field`, the latter because a local runtime has no
  inference-geography control to honour, so accepting the pin would misrepresent
  the agent's data-residency property. A malformed `speed` or `effort` is
  refused with its own code and the accepted value set rather than defaulted.

- Validates `web_fetch` and `web_search` domain lists against the published
  grammar on agent create/update and session creation. One list per entry, a
  non-empty list of at most 64 hostnames, no IP or internal host, no scheme,
  port, credentials, wildcard, or path on a `web_fetch` domain, no duplicates,
  and `max_content_tokens` or `user_location` only on the tool that accepts it.
  A rejected list answers `400 invalid_request_error` naming the list and
  zero-based index. Whether a web tool can execute is unchanged and still
  refused by capability admission.

### Highlights

- Adds the opt-in Pi CLI loop-engine foundation. New Pi sessions persist their
  selected engine, launch one restricted print-mode subprocess per text user
  turn, use a safe session file and private `models.json` with
  `$SANDBASE_PI_API_KEY`, and fail explicitly when the Pi CLI is unavailable.
- Translates validated Pi stdout JSONL into the same SQLite-backed CMA event
  log used by the builtin engine. Final text, native tool trajectory, spans,
  usage, retry/error state, and terminal completion are durable; text deltas
  remain transient and bounded stderr is redacted diagnostics only. Pi native
- Adds Pi session-file lease and SQLite-backed continuity proof. Live owners
  receive retryable busy responses; stale owners are recovered explicitly;
  mismatched/corrupt/refused resumes are visible errors. Pi cancellation,
  timeout, and unknown process-tree cleanup are distinct `cancelled`,
  `timed_out`, and `cleanup_pending` states, and retained workspaces are never
  released on uncertain cleanup.

### Fixes

- Returns the complete agent from an update that changes nothing, instead of a fabricated one. `PUT /v1/agents/{id}` whose body changed no field answered with an object that had `name`, `system` and `model` **absent** and `description`, `tools`, `skills` and `metadata` **zeroed** — `""`, `[]`, `[]`, `{}` — while keeping the right `id`, `status` and `version`, so it read as a real agent stripped of its name, prompt, model, tools, skills and metadata. The cause was one argument: the no-change branch passed `existing.definition` into `toApiAgent`, which reads properties off its argument, and `existing.definition` is the raw JSON **string** from the `agents.definition` column, so every read was `undefined` and each field fell back to its default. The `as never` cast at that call hid the type error from `npm run typecheck`, and the normal update path is correct because it passes the validated parsed definition — which is why the defect stayed in one branch. That branch is the one an **idempotent re-`PUT`** takes, so a client retrying an update after a timeout received a fabricated resource description even though the first call had succeeded. The response is now built the way `GET /v1/agents/{id}` builds it, from `parseAgentDefinitionFromRow`, so the two projections cannot diverge again; a stored definition that `GET` already refuses to serve answers the same `404` here rather than being projected into a well-formed-looking resource. Nothing else moves: this is a response fix, the no-change path still writes no row and mints no immutable version, and update semantics, `expected_version`, list replacement and metadata merge are untouched. The test compares the whole no-change body against the `GET` body for the same agent rather than against a fixture, and covers all three shapes that reach the branch — the stored value re-sent, an empty body, and a metadata deletion of a key that was never there. Found while verifying an audit claim; the official SDK does not reach it, because `ManagedAgentsClient.agents.update` sends a `GET`-then-`PUT` carrying every field, so the defect is reachable by any direct `PUT` on the wire.
- Starts the resume turn only once nothing is parked, so the published answer loop runs as written. The published client answers **every** entry of `stop_reason.event_ids` in turn — `会话事件流.md:1908` iterates the array straight back into `user.tool_confirmation`, and `:1929` / `:1955`-`:1957` do the same in Python and JavaScript — but the executor's resume gate considered only the confirmation group of the call being answered (`resolution.groupComplete`), so a custom tool call parked at the same time was invisible to it. The turn then started against a tool-result sequence that was still missing a call, the provider refused the request with `Tool result is missing for tool call <id>`, and the session was **terminated** for a documented, well-formed answer. Measured on the previous merge, driving the real strategy and the real status transition: answering one of two parked custom calls ended the session `failed` with `Tool result is missing for tool call custom_call_2`; a gated call answered while a custom call was parked failed on `call_gated`; a custom call answered while a gated call was parked failed on `custom_call_1`. Only the shape the gate was written for — two gated calls in one confirmation group — already worked. Each answer is now recorded as it arrives and the session stays in `requires_action` until the last parked call is answered, so a partial answer waits rather than failing. The gate and the `event_ids` array read one definition (`src/core/session/parked-calls.ts`), which is what keeps "the array is non-empty" and "no turn may start" from drifting apart: `lifecycleMetadataFor` now derives `event_ids` from the same function. The Pi loop engine is excluded on the same grounds as the existing confirmation step, because it resolves its own gate and appends the paired result when that call actually finishes — holding the turn there would leave the decision undelivered. No change to which events park a session, to which identifiers select a call, to confirm... (line truncated to 2000 chars) The published client answers **every** entry of `stop_reason.event_ids` in turn — `会话事件流.md:1908` iterates the array straight back into `user.tool_confirmation`, and `:1929` / `:1955`-`:1957` do the same in Python and JavaScript — but the executor's resume gate considered only the confirmation group of the call being answered (`resolution.groupComplete`), so a custom tool call parked at the same time was invisible to it. The turn then started against a tool-result sequence that was still missing a call, the provider refused the request with `Tool result is missing for tool call <id>`, and the session was **terminated** for a documented, well-formed answer. Measured on the previous merge, driving the real strategy and the real status transition: answering one of two parked custom calls ended the session `failed` with `Tool result is missing for tool call custom_call_2`; a gated call answered while a custom call was parked failed on `call_gated`; a custom call answered while a gated call was parked failed on `custom_call_1`. Only the shape the gate was written for — two gated calls in one confirmation group — already worked. Each answer is now recorded as it arrives and the session stays in `requires_action` until the last parked call is answered, so a partial answer waits rather than failing. The gate and the `event_ids` array read one definition (`src/core/session/parked-calls.ts`), which is what keeps "the array is non-empty" and "no turn may start" from drifting apart: `lifecycleMetadataFor` now derives `event_ids` from the same function. The Pi loop engine is excluded on the same grounds as the existing confirmation step, because it resolves its own gate and appends the paired result when that call actually finishes — holding the turn there would leave the decision undelivered. No change to which events park a session, to which identifiers select a call, to confirmation-group semantics, or to the one-shot rule for a single call.
- Addresses a custom tool call by its **event** id, and drops the local `action_type` field from `stop_reason`. The published contract parks a custom tool call exactly as it parks an approval — `agent.custom_tool_use` is emitted (`会话事件流.md:1875`), the session pauses with `stop_reason: requires_action` and the blocking event ids in `stop_reason.event_ids` (`:1876`), and the client answers with `user.custom_tool_result`, passing the event id in `custom_tool_use_id` (`:1877`) — but the runtime was entirely outside that exchange. `lifecycleMetadataFor` scanned only `agent.tool_use` / `agent.mcp_tool_use`, so a session paused on a custom tool call published an **empty** `event_ids` and a conforming client had no id to answer with; and `getCustomToolResultMetadata` matched the `tool_use` **block** id alone, so even a caller that found the right event id elsewhere was refused with "custom_tool_use_id does not reference a pending custom tool call". A session waiting on a custom tool was therefore unreachable through the documented loop. The pending scan now covers unanswered `agent.custom_tool_use` events and reports their own ids, a `user.custom_tool_result` may name one, and the block id stays accepted as a documented local fallback. The persisted `metadata.custom_tool_use_id` is still the **block** id, because the model-facing projection pairs a custom tool result to its call by tool-call id, and a call answered by either spelling is excluded from the array and cannot be answered twice. `action_type` — written here, declared in the protocol type, absent from the published contract and read by nothing — is no longer projected: with both families parked at once no single value of it was true, so the object is now exactly the published `{type, event_ids}`.
- Addresses an approval-gated tool call by its **event** id, which is the id the published contract circulates. `stop_reason.event_ids` reported the `tool_use` **block** id, and both the confirmation validator and the executor matched that same block id, so a conforming client read an id out of `event_ids`, sent it back exactly as the contract instructs — "在 `tool_use_id` 参数中传递事件 ID" — and was refused with `Invalid tool confirmation: the tool call is not awaiting approval`. The published approval loop could not complete against this runtime at all. `event_ids` now names each pending `agent.tool_use` / `agent.mcp_tool_use` event by its own id, and `user.tool_confirmation` accepts that id; the `tool_use` block id is still accepted as a documented local convenience, because this runtime's own callers answer with one. Resolution, the duplicate check, the appended `agent.tool_result` block, and the confirmation event's `metadata.tool_use_id` all stay keyed on the block id, so the model-facing projection still pairs a tool result to its call and answering with the other spelling is not a way to decide one call twice. Widening which identifier *selects* a call does not widen authority: the pending check and the one-shot resolution still decide whether anything runs.
- Exposes the session-level `stop_reason` object at the top level of `session.status_idle`, where the published client reads it. `src/types/cma-protocol.ts` declares `SessionStatusIdleEvent.stop_reason` as a top-level object, but the runtime persisted it inside the generic metadata carrier and `toApiEvent` never lifted it, so `stop_reason` was `undefined` at the top level of every idle event. The published loop reads it there — `jq -r 'select(.type == "session.status_idle") | .stop_reason.type // empty'` — and with the object nested that expression returned the empty string every time, so its `case` arm matched neither `requires_action` nor `end_turn` and a conforming client could not tell "the session paused and needs my answer" from "the turn ended", including the documented interrupt case where the interrupted turn reports `end_turn`. The object is now projected for `session.status_idle`, and `metadata.stop_reason` keeps it unchanged because `src/api/routes/runs.ts` reads the persisted path to choose between a `202` with `wait_deadline_reached` and a terminal body. Model-derived events keep the provider's `stop_reason` **string** from the `events.stop_reason` column; the two shapes share the field name because both published shapes spell it `stop_reason`, and they are told apart by event type. An idle event with no reason omits the field rather than sending `null`. Both halves of the exchange were then brought into line in the two fixes above: `event_ids` names the parked calls by their own event id, and a confirmation or a custom tool result may address a call by it.
- Projects the declared top-level fields of every tool event onto the wire. A tool event persisted its payload in `content[0]` only, while `src/types/cma-protocol.ts` declares `name` and `input` as top-level fields on `agent.tool_use`, `agent.mcp_tool_use` and `agent.custom_tool_use`, `tool_use_id` on `agent.tool_result`, and `custom_tool_use_id` on `user.custom_tool_result`. A client written against the declared type therefore read `undefined` for a value the type promised, and the published tool loop could not run at all: it resolves a blocking event id from `stop_reason.event_ids`, then reads `toolEvent.name` and `toolEvent.input` off the event it found. `toApiEvent` now lifts those fields, and `content` is unchanged — the projection adds fields rather than moving them. The top-level `id` deliberately stays the persisted **event** id, because that is the value the same loop answers with; the tool-call id remains reachable as `content[0].id`. A field the block does not carry is omitted rather than sent as `null`, and no field is projected onto an event type whose declared shape has none.
- Answers a path this runtime does not serve with the structured error envelope instead of the framework's plain-text body. An unmatched request returned HTTP 404 with `content-type: text/plain` and the body `404 Not Found`, while every 404 this runtime raises itself returned `application/json` with `error.type: "not_found"`. A client that decodes `error.type` therefore failed while parsing and reported a transport-shaped error for what is really a not-found, and the two shapes differed by which layer noticed the miss rather than by anything the caller did. The server now registers one fallback, so an unrouted path in any namespace answers `{"error":{"type":"not_found","message":"No route matches this request"}}` as JSON. The fixed message does not echo the requested path and stays distinct from a route's own missing-resource message, so "wrong path" and "wrong id" remain tellable apart. Authentication, throttling, and compatibility admission are mounted before routing, so an unmatched `/v1/*` path still answers `401`, `429`, or an admission `400` when those apply. Mounted routes, their 404 bodies, and the Console surface are unchanged.
- Warns at startup when `config.yaml` model settings are not the ones in effect. The file is a first-start import: it seeds the workspace model provider and, from that, the saved Settings document, and every later start reads the saved document. An operator who edited `model.base_url` in `config.yaml`, restarted, and saw `GET /v1/x/runtime` keep answering with the old endpoint got no explanation, and nothing distinguished the two write paths. Startup now names the difference and which source wins, and names the two `model` keys the import reads and never applies — a concrete `model` id, because an Agent declares its own, and the vendor `options` bag, because the model record this file seeds has no column for one. An agreeing file stays quiet, and only whether an API key was declared is reported, never its value.
- Addresses a `vendor/model` model id to the provider whole instead of truncating it. Every slash-containing model name was split into a provider prefix and a model id, so an endpoint that addresses its models by vendor namespace — OpenRouter and the many OpenAI-compatible gateways shaped like it, including a gateway whose ids are all `vendor/model` — could not be addressed at all: `deepseek/deepseek-v4-flash` reached the upstream as `deepseek-v4-flash`, and the request failed with a 404 for a model that does not exist. A leading token is now read as a provider selector only when some configured provider answers to it (by provider type or by registry name); otherwise the whole reference is the model id and travels unchanged. A reference whose namespace a first-party vendor API cannot serve is still refused rather than forwarded — `api.anthropic.com` cannot answer for `openai/gpt-5.5` — while an `openai_compatible` endpoint is treated as the routing authority for its own namespaces, so a router keeps serving `anthropic/...` and `deepseek/...` side by side. `model_used` and the usage and cost records projected from it now name the id the provider was actually asked for, recorded against the client the registry built, rather than the agent's raw reference.

- Reports a model failure as the configuration mistake it is. A missing model, a provider the workspace has not configured, an unusable provider configuration, and a refused credential all raised the same `internal_error` and moved the session to `failed`, which publishes `session.status_terminated` — a client could not tell a fixable typo from a crashed runtime, and the conversation was reported as over. They now carry the distinct codes `model_not_found`, `model_provider_not_configured`, `model_config_invalid`, and `model_auth_failed`, a provider 404 and 401/403 are classified from their structured status rather than from a string match on the message, and `retry_status` is `not_retryable` for each. A turn that fails with one of them leaves the session idle and resumable, so correcting the provider or the agent's model id and sending the next event is enough.

- Corrects the `docs/api.md` Webhooks section, which still described the published header set as first-attempt-only and a retry as carrying the legacy body signature alone. #314 changed that behaviour and updated the contract, the capability matrix, `docs/api-matrix.md` and this changelog, but missed this passage, so the page documented the reverse of what the runtime does.

- Carries the published webhook delivery headers on every attempt. `attemptDelivery` passed a delivery identity to `postWebhook`, and that identity is what adds `webhook-id`, `webhook-timestamp` and `webhook-signature` beside the legacy body signature; `retryDelivery` called the same helper without one, so a retry sent the legacy signature alone and a receiver that verifies the published set had nothing to verify on a retry. A retry now keeps the delivery id — one delivery stays deduplicable across attempts — and signs `<id>.<this attempt's timestamp>.<body>` with the delivery secret, which is what keeps a receiver's freshness window satisfied. The recorded `signature` column still holds the legacy value, so its meaning does not change.

- Anchors the inbound `/v1` rate-limit window to the request that opened it
  instead of to the wall-clock minute. Every bucket used to be cleared at each
  minute boundary, so two writes 10 ms apart that fell on opposite sides of
  `12:01:00` were both allowed and a one-write budget was spent twice inside 60
  seconds. A window now runs a full minute from its first counted request, so a
  burst straddling a boundary is throttled, and `Retry-After` reports the time
  until the caller's own window expires rather than the time to the next minute.
  Buckets, budgets, exemptions, and the 429 body are unchanged.

- Keeps OpenAI-compatible streaming sessions alive when a gateway fragments a
  tool call across many SSE deltas and emits an empty or missing
  `tool_calls[].type`. The runtime rewrites only that field on the wire, so
  argument fragments and every other byte are preserved and the stream no
  longer aborts with a schema validation failure. Non-SSE and error responses
  are passed through untouched.

- Makes approval-gated tool calls durable and atomic per model step. Every
  confirmation decision, group identifier, and paired tool result is stored in
  the append-only event log; a model continuation begins only after the full
  group is resolved. Sessions expose `requires_action` directly while pending.
- Makes Console session replay sequence-aware. The Console now treats the
  resumable event tail as the single durable source, merges REST snapshots by
  sequence, and never advances a replay cursor for transient text chunks.
- Adds `seq` and immutable `metadata` to public event responses and aligns SSE
  envelopes with REST. Model-attributed event projections include the selected
  model and provider stop reason when available.
- Records the session budget capability as `planned` instead of `partial`. The
  matrix entry claimed enforcement, pause semantics, the settlement-event
  whitelist, a session-wide ceiling and an operator-supplied cost profile, while
  `session-budget.ts`, `cost-profile.ts`, and their tests are absent from the
  tree: nothing priced model consumption and no session accepted a budget. The
  contract documents disagreed with each other as well, `sessions.md` calling the
  budget `not_applicable` and `unsupported.md` calling it `partial`. The entry and
  both documents now state that the design is published and nothing implements it.

- Holds the SQLite state database to the connection settings the runtime was
  already assuming. `src/core/db/database.ts` opened the workspace database with
  `journal_mode = WAL` and `foreign_keys = ON` and nothing else: no
  `busy_timeout`, no `synchronous`, no `trusted_schema`, and no read-back, so a
  pragma that did not take effect — on another platform, another Node version or
  another driver — left the runtime running with a weaker guarantee than it
  reported. A connection now sets and then verifies
  `journal_mode=WAL`, `foreign_keys=ON`, `busy_timeout=5000`,
  `synchronous=FULL` and `trusted_schema=OFF`, and a value that does not read
  back refuses startup instead of being logged and ignored. The two macOS-only
  pragmas in the recipe this follows (`fullfsync` and `checkpoint_fullfsync`) are
  deliberately not claimed: Node's bundled SQLite defines no
  `SQLITE_ENABLE_FULLFSYNC`, and the pragma is a flag that reads back as set on
  any platform, so verifying it could not show the guarantee was in effect, while
  `synchronous=FULL` is the durability guarantee that applies here. An
  in-memory database is accepted with the one journal SQLite can give it, and
  every other setting is verified for it too. `Database.transaction()` now opens
  with `BEGIN IMMEDIATE`, so the write lock is taken when the transaction starts
  rather than at its first write: with the CLI and a server pointed at the same
  file, a deferred transaction could hold a read snapshot that another process's
  commit made unpromotable, which failed with `SQLITE_BUSY_SNAPSHOT` after the
  transaction body had already run. The same conflict is now a bounded wait.
  Existing workspaces need no migration — the settings are connection-level and
  the WAL conversion happens on open.

### Security

- Redacts credential secrets from values that are logged or returned. A redactor
  bound to one injection bundle replaces every secret it holds wherever it appears,
  including nested objects and arrays, and `clear()` drops the held secrets so the
  redactor cannot be reused after the turn ends. A secret belonging to a different
  bundle is not redacted, and a value containing no secret is returned unchanged.
  The canonical credential shape rejects an unknown field rather than ignoring it.

- Stops persisting raw model reasoning in the public event log. `agent.thinking`
  is now emitted as a content-free progress signal carrying
  `metadata.signal = "reasoning"`, so reasoning traces that echo tool output are
  no longer readable through the REST event listing or the SSE stream.
- Adds credential-scoped, in-process inbound rate limiting after authentication.
  Protected runtimes use independent read/write fixed windows, structured 429
  responses, health/preflight exemptions, dynamic managed-key posture, and
  environment overrides; open local runtimes remain unlimited by default.

- Enforces credential vault network policy at the injection boundary. Limited
  credentials require a verified matching target host before decryption; denied
  credentials are omitted from injection outputs, audited without secret values,
  and do not update `last_used_at`. This helper boundary does not yet claim
  universal web/MCP/custom-tool caller propagation.

- Admits CMA `/v1` requests that use `x-api-key` or Anthropic compatibility
  headers before route business logic: required version/beta headers are
  validated with stable structured errors, memory-store routes require their
  own memory beta, and existing `Authorization: Bearer` requests without CMA
  headers retain their behavior. Authentication accepts exactly one credential
  source per request; dual `Authorization` and `x-api-key` inputs are rejected.
  Memory-store requests reject combined managed-agents and agent-memory betas,
  while the documented memory listing accepts either beta.
- Updates `prefix-safe-json` `0.4.2` -> `0.4.3`, a security release fixing
  GHSA-3xpw-9694-2xxp: the AI SDK adapter used to silently drop raw stream
  events once it had already observed a call's own terminal, and
  `takeDecision()` used to read a decision snapshot frozen at `finish()`
  time instead of live coordinator diagnostics, so late or contradictory
  lifecycle evidence for a tool call could fail to revoke its execution
  authority. No public API change. Confirmed the exact vulnerable-then-fixed
  behavior against this integration's own `createAiSdkV4ExecutionGuard`
  wrapper (`tests/integration/post-terminal-authority.test.ts`), not just
  the library's own test suite.

- Stops an Environment's hosting declaration from silently becoming local
  execution. `hosting_type` was written but never read by the resolver, so an
  Environment that asked for `cloud`, `docker`, or `kubernetes` and did not also
  name a `sandbox_provider` ran on the local backend — the one backend that is
  not a security boundary — and an unusable declaration was indistinguishable
  from no declaration at all. An Environment is now resolved fail-closed:
  `cloud` names hosting on machines this runtime does not own, has no backend
  here, and is refused rather than lowered to local; an unknown hosting type is
  refused with `unsupported_hosting_type`; a config that is not valid JSON, not a
  JSON object, or that declares a hosting field which is not a name is refused
  with `invalid_environment_config`; and a declared
  `docker`, `kubernetes`, or `self_hosted` hosting type now selects that backend.
  An explicit `sandbox_provider` continues to win over `hosting_type` and is
  preserved verbatim, so an out-of-tree provider still reaches the registry,
  which rejects an unregistered name by name. The refusal is raised when the
  session is created or when an event is admitted — before any row is written
  and before the session can accept work — and is served as a `400` carrying the
  stable error code, instead of surfacing later as a provisioning failure. The
  workspace default seed refuses the same inputs rather than reducing them to
  local. Read paths are unaffected: `GET /v1/environments` reports the backend an
  Environment declares, so a Kubernetes Environment is no longer projected as
  `cloud`, and a declared hosting type is echoed even when this build refuses to
  execute it, so an operator can still see what the record says.

### Documentation

- Documents the verified git-hosted DSH install flow: the first add fails with
  `ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED` until the exact key pnpm prints is
  added under `allowBuilds:` in the profile's `pnpm-workspace.yaml`; the
  second add runs `prepare`, builds `dist/`, creates both bins, and joins the
  bundle layer. Verified end to end on Windows with DSH `0.1.1-rc.2`.

### Console

- Shows the effective permission policy on each toolset header of the agent page. The policy is not visible in the definition — an `mcp_toolset` requires approval even when no `default_config` is written — so the page previously showed an MCP server's name and nothing about how its tools would be treated, and hardcoded "Always allow" for the built-in toolset. An operator can now tell a gated third-party server from an ungated one, and a reader checking the claim that the Console shows "Always Allow" has UI evidence for it. The badge carries the policy in its text and in a class name, so the policies are distinguishable without relying on colour alone.
- Improves the staged conversation experience with streaming message
  projection, resource selection, safe Markdown rendering, tool confirmation
  states, and event metadata needed by the Console.

## 0.3.8 - 2026-08-30

### Fixes

- Builds missing `dist/` entries during git-hosted installs through `prepare`,
  so DSH can install `git+https://github.com/sandbaseai/sandbase-harness.git`
  without a prior local `npm run build`. Published packages that already ship
  `dist/` skip the rebuild. MCP image builds install dependencies with
  `--ignore-scripts` because the Dockerfile copies `package.json` before
  `scripts/`.
- Counts model usage once per model request in runtime metrics and persists
  aggregate input/output token usage on each session.
- Validates confirmed tool calls against raw AI SDK v4 stream lifecycle data,
  rejects malformed or incomplete calls, and grants one-shot execution
  authority only after validation.
- Documents the maintainer and organic project-promotion workflow in
  `AGENTS.md` and keeps the DeepSeek Harness Handbook discovery link current.

## Unreleased

## 0.3.7 - 2026-08-20

### Fixes

- Makes the DeepSeek Harness bundle boot in a clean Web profile when
  `MANAGED_AGENTS_API_KEY` is unset by keeping all MCP `env` values string-typed.
- Resolves the MCP entry from the installed profile instead of requiring a
  globally linked `managed-agents-mcp` executable on `PATH`.
- Replaces the ambiguous npm-name installation command with a pinned local
  source install, preventing resolution of the unrelated unscoped package.

## 0.3.6 - 2026-08-20

### Fixes

- Restores the documented `/v1` operations API for webhooks, scheduled
  deployments, and outcomes while retaining `/v1/x` compatibility aliases.
- Exposes all three operations screens in the primary Console navigation and
  routes `#scheduled-deployments` to its intended page.
- Wires the complete Memory Stores implementation to prevent list and detail
  views from crashing on missing runtime bindings.

## 0.3.5 - 2026-08-20

### Highlights

- **Agent Skills compatibility metadata**: validates and preserves the standard
  optional `compatibility` field (1–500 characters), exposes it through the API,
  and renders it safely in Console search and detail views.
- **Instant Codespaces evaluation**: adds a development-container configuration
  and documented one-click path to build and open the local Console without
  preparing a host Node.js environment.
- **Verified DSH onboarding**: adds runtime/auth preflight checks and links the
  bilingual DeepSeek Harness developer walkthrough and operator tools.
- **Release-aligned Agent Plugin**: pins the portable MCP plugin to the v0.3.5
  multi-architecture bridge image produced from this tag.

## 0.3.4 - 2026-08-19

### Highlights

- **Published MCP container**: release tags now produce public multi-platform
  `linux/amd64` and `linux/arm64` images in GitHub Container Registry.
- **Verifiable supply chain**: images carry OCI and MCP ownership labels and a
  GitHub build-provenance attestation tied to the release digest.
- **Registry-ready metadata**: adds a version-aligned `server.json` for the
  official MCP Registry OCI distribution format.

## 0.3.3 - 2026-08-19

### Highlights

- **First-class MiniMax provider**: adds MiniMax to Settings V2 and the
  runtime provider boundary, with global and mainland China endpoints.
- **Current MiniMax models**: supports `MiniMax-M3` as the default and
  `MiniMax-M2.7` as an explicit alternative.
- **Verified setup guide**: documents environment-variable credentials,
  regional endpoint selection, agent model IDs, and configuration checks.

## 0.3.2 - 2026-08-15

### Highlights

- **Containerized MCP bridge**: adds `Dockerfile.mcp` and `.dockerignore` for
  the six-tool stdio bridge, with OCI and MCP server metadata.
- **Verified container build**: CI now builds the production MCP image from a
  clean checkout so broken Docker packaging blocks future changes.
- **Release-aligned quick start**: source, Docker, installation, deployment,
  and DeepSeek Harness examples now pin the immutable v0.3.2 tag that contains
  every referenced file.
- **Project trust and discovery**: adds security and contribution policies,
  private vulnerability reporting, and a direct SandBase Agent Skills link.

## 0.3.0 - 2026-08-14

### Highlights

- **DeepSeek Harness integration**: ships an installable Cordis bundle and the
  `managed-agents-mcp` stdio server so DSH can create and inspect agents,
  manage persistent sessions, stream turns, retrieve artifacts, and cancel
  runs through MCP.
- **Verified integration guide**: adds a reproducible DSH configuration,
  permission boundaries, troubleshooting guidance, and compatibility evidence
  against DeepSeek Harness commit `47f9438`.
- **DeepSeek V4 reasoning controls**: forwards Settings V2 `reasoning_effort`
  values to OpenAI-compatible model requests and documents a verified
  DeepSeek V4 setup.
- **Distribution metadata**: corrects package repository links to
  `sandbaseai/sandbase-harness` and adds Glama maintainer metadata for MCP
  directory verification.

## 0.2.0 - 2026-08-01

### Breaking changes

- **Workspace state directory moved**: runtime state (database, config, logs)
  now lives at `<workspace>/.managed-agents/` instead of
  `~/.managed-agents/<name>-<hash>/`. Existing workspaces need their state
  moved manually or will re-initialize on next start.
- **`composeRuntimeFromSettings`**: the `memorySeedEnabled` parameter was
  removed; use `settingsSeed: { memoryEnabled: true }` instead.
- **Legacy provider endpoints permanently removed**: `/v1/x/model-providers`,
  `/memory-providers`, `/storage-providers` return 404. Use
  `/v1/x/settings` to configure providers.

### Highlights

- **Runtime decomposition**: the monolithic route and session files are split
  into focused modules (environments, files, credential-vaults, memory-stores,
  runtime, settings, templates, session-normalizers, session-stream,
  session-records, session-lifecycle, session-recovery, secrets,
  skill-packages, skill-resources, resource-utils). `resources.ts` and
  `extended.ts` are now pure composition roots.
- **Sandbox backends**: local process, Docker (with per-session container
  labels and path confinement), and Kubernetes (kubectl exec/cp transport)
  are shipped; Environment write-side validation rejects unknown providers.
- **Docker environment mode**: `hosting_type: docker` with image/resources
  fields in the Environment config.
- **Console improvements**: YAML/JSON agent config editor, fix modal crashes,
  session send failure handling, environment editor polish, settings form
  splitting.
- **Release gate**: `npm run release:check` covers typecheck (src + tests),
  full test suite, production build, package dry-run, CLI init smoke, and
  example workspace startup.
- **Model registry**: `resolveModelConfig()` supports `openai/gpt-5.5`-style
  qualified model references.
- **Workspace registry**: `managed-agents init` and
  `createRegisteredWorkspace` write config to `.managed-agents/config.yaml`,
  consistent with the runtime's path resolution.

### Stats

- 75 test files, 569 tests (up from 67 / 544 in 0.1.0).
- 24 new source modules, 7 new test files.
- `release:check` passes on a clean checkout.

## 0.1.0 - 2026-07-18

First public release of `managed-agents`.

### Highlights

- Local-first managed agent runtime with a Claude Managed Agents-style `/v1`
  API surface.
- React Dashboard for agents, sessions, environments, credential vaults,
  memory stores, files, skills, settings, logs, monitoring, and API reference.
- SQLite-backed runtime state stored outside source-controlled workspaces by
  default.
- Settings V2 for one workspace model vendor, loop engine, storage backends,
  context-memory backend, sandbox provider, API keys, validation, and restart
  flows.
- Session lifecycle, event replay, resumable SSE streams, memory resources,
  file resources, credential vaults, snapshots, local/Docker/self-hosted
  sandbox registration, MCP tools, and skill packages.
- TypeScript SDK and CLI commands for init, start, list, reload, chat, deploy
  guidance, and templates.
- Release gate covering typecheck, tests, production build, package dry-run,
  CLI init smoke, and example workspace startup smoke.

### Known first-release boundaries

- One active model vendor, one built-in loop engine, SQLite metadata storage,
  local artifact storage, SQLite memory, and runtime-registered sandbox
  providers are supported in 0.1.0.
- Planned adapters such as S3, Postgres/MySQL, mem0, MemU, Harness, Codex, and
  Claude loop engines remain unavailable until their runtime implementations
  are added.
- Live remote model credential checks and production deployment examples are
  tracked as follow-up work.
